Que sí, que ya sé que el amor es ciego. Pero yo no, joder

Social Engineering: Because there is no patch for human stupidity.

Nobody wants to say how this works.  Maybe nobody knows ...   >> "VIDEOADAPTOR SECTION" on xorg.conf manual ファミコン

"This is Unix-Land. In quiet nights, you can hear the Windows machines reboot"

to download < team speak >

"Es prácticamente imposible enseñar programación correctamente a estudiantes que han estado expuestos al lenguaje BASIC con anterioridad. Como potenciales programadores, tienen la mente mutilada sin esperanza alguna de regeneración" Edsger Dijkstra.



No extraña que los explotadores de profesión, hayan decidido incluir en sus actividades a la media
humanidad que les faltaba, pero sorprende que algunas mujeres consideren deseable esta situación:
 además del castigo bíblico propio, quieren pasar por el que les cayó a los hombres. Y, encima, en
 modo alguno comprenden que sea un castigo, una situación nada deseable.
 Como si fueran calvinistas se portan, convencidas de que el trabajo enriquece.
 Los hombres, más entrenados, sabemos de antiguo que sólo es posible enriquecerse haciendo trabajar
  a los demás.

SOFTWARE: Es el espíritu insuflado en la dura mollera de la máquina. A veces, un clásico espíritu
burlón o poltergeist.
														Arturo Rosby

"«Manos a la obra, señor Freeman, manos a la obra. No quiero decir que se haya
dormido en los laureles en el trabajo. Nadie se merece más un alto en el camino y todos los
esfuerzos realizados habrí­an sido en vano hasta que... Hmmm... Bueno... Digamos que
otra vez ha llegado su hora. El hombre adecuado en el sitio equivocado puede cambiar
el rumbo del mundo. Despierte, señor Freeman. Despierte y huela las cenizas»
														G-Man al comienzo de Half Life 2."

Los Grigori (del griego egrḗgoroi, que significa Observadores o vigilantes), también conocidos como
 hijos de Elohim (en hebreo בני האלהים, bnei ha'elohim);
 son un grupo de ángeles caídos de la mitología judeocristiana mencionados en algunos textos
 apócrifos Bíblicos y en el Libro del Génesis.

En estos textos se menciona que los Grigori fueron seres que se aparearon
con las "hijas del hombre" (en hebreo banot ha'adam);
naciendo de esta unión una raza de gigantes conocida como los Nephilim.

"Si los tontos volaran, el cielo se oscurecería"

"Recuperar alkalinidad en el cuerpo"
2 partes de agua
1 parte de jugo de limon
1 pizca de carbonato

Aviso: Este e-mail es confidencial y no debería ser usado por nadie que
no sea el destinatario original. No se permite la reproducción mediante
fotocopia, walkie-talkie, emisora de radioaficionado, satélite,
televisión por cable, proyector, señales de humo, código morse, braille,
lenguaje de signos, taquigrafía o cualquier otro medio. Bajo ningún
concepto debe traducirse al francés este e-mail. Este e-mail no puede
ser ridiculizado, parodiado, juzgado en una competición, o leído en voz
alta con un acento gracioso llevando un bigote falso y/o cualquier tipo
de sombrero, incluyendo pero no limitándose a pañuelos. No inciten ni
provoquen a este e-mail. Si está medicándose, puede experimentar
nauseas, desorientación, histeria, vómitos, pérdida temporal de la
memoria a corto plazo y malestar general al leer este e-mail. Consulte a
su médico o farmacéutico antes de leer este e-mail. Todas las modelos
descritas en este e-mail son mayores de 18 años. Este e-mail se reserva
el derecho de admisión. Si ha recibido este e-mail por error es
probablemente porque estaba borracho cuando escribí la dirección del
destinatario.

"Vade retro satana"


The Latin text says:

Crux Sancti Patris Benedicti / Cruz del Santo Padre Benito
Crux Sacra Sit Mihi Lux / Mi luz sea la cruz santa
Non Draco Sit Mihi Dux / No sea el demonio mi guía
Vade Retro Satana / ¡Apártate, Satanás!
Numquam Suade Mibi Vana / No sugieras cosas vanas
Sunt Mala Quae libas / Pues maldad es lo que brindas
Ipse Venena Bibas / Bebe tú mismo el veneno

Crux sancta sit mihi lux / Non draco sit mihi dux
Vade retro satana / Numquam suade mihi(mibi) vana
Sunt mala quae libas / Ipse venena bibas
In approximate translation:

Let the Holy Cross be my light / Let not the dragon be my guide
Step back Satan / Never tempt me with vain things
What you offer me is evil / You drink the poison yourself.

La Santa Cruz sea mi luz, /no sea el dragón mi señor.
¡Apártate, Satanás! /nunca me atraigas con engaños,
maldad es tu carnada, /bebe tu propio veneno.

INRI >> Iesus Nazarenus Rex Iudaeorum

#########################################################################################

どうもありがとうミスターロボット (dōmo arigatō misutā robotto)

##################
My firts name
Hiragana      katakana
へすす         ヘスス
My second name
Hiragana      katakana
あるべると    アルベルト

Her name  firts A then Y
Hiragana      katakana
あれはんどら  アレハンドラ

やみれと      ヤミレト

###############Remenber this tools on your journey
chntpw                NT SAM password recovery utility
ms-sys                Create DOS/MS-compatible boot records
foremost              Recover files by "carving" them from a raw
hdparm                Utility to get/set (E)IDE/SATA/SAS device parameters
ide-smart             IDE S.M.A.R.T. test and query tool
john                  Password cracker
memtester             Userspace utility to test for faulty memory subsystem
testdisk              Tool to check and undelete partition
targa                 MS Windows95/98/NT testing kit
scrounge-ntfs         Data recovery program for NTFS file systems
dmidecode         dumping bios information
galleta - An Internet Explorer cookie forensic analysis tool
pasco - An Internet Explorer cache forensic analysis tool

lm-sensors		check temp of your box
smartmontools		smart-monitor
cpuburn 		stress your cpu
			run desired program in background [ _repeat_ for SMP]:
			`burnP6 || echo $? &`
surfraw     		example surfraw -browser=w3m google "query"
gotmail			download e-mail from hotmail
wapua			web browser for wap
fcrackzip		pass-cracker
fondu			convert between mac fonts format
encfs			encrypt fs ?
electricsheep		just awesone !
fatresize		--
archmage 		extract and convert chm files to html , txt or pdf
flasm			assembler and disassembler for flash-(swf) bytecode
badblocks               for cheking bad blocks
xsw			presentations sowftware very easy to use
############################################################################################
sodexo
6275350035284037

"the Curro"
sa : #integrabd20120911
zam : lis


"rdesktop"

#database
rdesktop -a 16 -x 1 -g 1600x840 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -u SistemasTBK -p S1st3m5s5 192.168.20.235 -k es -r disk:sharename
#exchange
rdesktop -a 16 -x 1 -g 1600x840 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -d INTEGRA -u Administrator -p P1$$w0rd 192.168.1.91 -k es -r disk:sharena
#AD
rdesktop -a 16 -x 1 -g 1600x840 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -d INTEGRA -u jose.pulidob -p S1st3m5s 192.168.1.90 k es -r disk:sharena
#Domain name
rdesktop -a 16 -x 1 -g 1600x840 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -d INTEGRAMAIL -u teisa -p P1$$w0rd 192.168.1.89 -k es -r disk:sharena
#ATM
rdesktop -a 16 -x 1 -g 1600x840 -r sound:loff -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -d ATMDOM2009 -u administrador -p S15t3m45M15 192.168.28.108 -k es -r disk:sharena
#MR
rdesktop -a 16 -x 1 -g 1600x840 -r sound:loff -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -d INTEGRA -u jesus.mendozab -p @Effeta1# 192.168.20.238 -k es -r disk:sharena

"Applications Bonampak"
\\192.168.20.236\Microsoft Dynamics


"CAKEPHP 1.3 SECTION"
MainelyDesign.com Blog
Pagination with MSSQL in CakePHP

Posted on 05/07/2009 at 04:36 pm by Kevin Wentworth
Viewed 10,615 times | 0 comments

One thing, and there are a few, that I can't stand about having to use PHP and MSSQL Server together is the lack of support for pagination.  With PHP and MySQL- no problem use limit.  MSSQL- can't do it without all these ugly sub-queries.  I have to give it to the CakePHP developers though, for the built-in support for pagination in MSSQL.  Albeit it is flawed, it works.

The problem with the CakePHP support for pagination in MSSQL is that on the last page of results you get a full result set of whatever you've set as your records per page.  If you have 34 records and you are paginating 20 per page, you'll see 1-20 on the first page, but you'll see 14-34 on the second page.  This is due to a limitation of MSSQL prior to 2005.  Since we've recently upgraded to using MSSQL 2005 for the project I need to use MSSQL for, I decided to give this patch and try.  I recommend it to all MSSQL 2005 and up users.  It's almost as good as using MySQL.

It's pretty simple to add to your dbo_mssql.php file.  Yes, it hacks the core, but I think this functionality will be added in CakePHP 1.3 anyway.  All that's missing is a way to tell what version of MSSQL is being used and modify the pagination query appropriately.

In dbo_mssql.php (line 549):
Show Plain Text

    return "SELECT * FROM (SELECT {$limit} * FROM (SELECT TOP {$offset} {$fields} FROM {$table} {$alias} {$joins} {$conditions} {$group} {$order}) AS Set1 {$rOrder}) AS Set2 {$order2}";

Change to:
Show Plain Text

    $limitint = (int)$offset + 1 - (int)trim(str_replace('TOP','', $limit));
    return "SELECT * FROM (SELECT row_number() OVER ({$order}) as resultNum, {$fields} FROM {$table} {$alias} {$joins} {$conditions} {$group}) as numberResults WHERE resultNum BETWEEN {$limitint} and {$offset} ";

Thanks to celsowm for posting the patch (#5675).

Cheers,

-Kevin Wentworth
"CAKEPHP 1.3 SECTION"


"Supported framebuffer mode "
hwinfo --framebuffer | grep Mode

"setting OS-clock system and bios-clock system"
firts set the OS clock
date --set "yyyy-mm-dd hh:mm"
then set the bios clock
hwclock --set --date="yyyy-mm-dd hh:mm"
done

"mount a device by UUID"

ls -l /dev/disk/by-uuid/

// add a fstab example
UUID=AADA5AF3DA5ABB71 /media/mssql_backup_disk ntfs-3g auto,silent,users,noatime,allow_other,umask=0,uid=systems,gid=users,dmask=022,fmask=133 0 0
// if something goes wrong on mounting

systemctl daemon-reload

/** Mercenary Forensics */

"Finding all files containing a text string on Linux"
find / -type f -exec grep -H 'text-to-find-here' {} \;

egrep -w -R 'word1|word2' ~/projects/

"find files and copy with directory structure"

find ./ \( -iname "*.docx" -o -iname "*.doc" -o -iname "*.xlsx" -o -iname "*.xls" \)  -exec cp -r --parents {} /disk/mercenary/rh \;


"scalpel"
Saving the Sandman

Next, I’ll give some examples of bailouts. Scenario 1 involves a household without TV with at least one young child. The father has accidentally deleted the child’s favorite “Sandmann” episodes, which were recorded from ARD Mediathek over several days. Deprived of the show, the child expresses disappointment in the usual loud and unmistakable way.

The scalpel.conf file does not have an entry for the MP4 format, but the existing – long-since

ambagasdowa@uruk:~/Documents/recovery/Fakepath/FIRMA DIGITAL 2014-16 MEXICALI/2016$ hexdump -C -n 20 LLAVE2016MEX.key ; hexdump -C -n 20 LLAVE2016MEX.req ; hexdump -C -n 20 ../LLAVE2014.key ; hexdump -C -n 20 ../LLAVE2014.req
00000000  30 82 02 a0 30 1a 06 09  2a 86 48 86 f7 0d 01 05  |0...0...*.H.....|
00000010  03 30 0d 04                                       |.0..|
00000014
00000000  30 82 02 94 30 82 01 fd  02 01 00 30 82 01 54 31  |0...0......0..T1|
00000010  25 30 23 06                                       |%0#.|
00000014
00000000  30 82 02 a0 30 1a 06 09  2a 86 48 86 f7 0d 01 05  |0...0...*.H.....|
00000010  03 30 0d 04                                       |.0..|
00000014
00000000  30 82 02 99 30 82 02 02  02 01 00 30 82 01 59 31  |0...0......0..Y1|
00000010  25 30 23 06                                       |%0#.|
00000014


Dad now feeds the data to scalpel.conf. The first item in the new entry (Figure 2) is the file extension that should receive potential matches.


#---------------------------------------------------------------------
#key and req files shit of sat
#---------------------------------------------------------------------
        key     y     1000:10000    \x30\x82\x02\xa0\x30\x1a\x06\x09\x2a\x86\x48\x86\xf7\x0d\x01\x05\x03\x30\x0d\x04
        req     y     1000:10000    \x30\x82\x02\x99\x30\x82
        req     y     1000:4000     \x30\x82\x02\x94\x30\x82


The “y” indicates whether Scalpel distinguishes between uppercase and lowercase in the header and footer. This is followed by the minimum and maximum file sizes – the MP4s usually occupy between 30 and 70MB. Finally, the header is given. A footer cannot be specified, because it always turns out differently.

Then, dad starts the rescue operation at the command line with:
scalpel -c scalpel.conf -o sandmann_recovered /dev/sdd1

scalpel -c Documents/recovery/scalpel.conf -o /disk/recovery/rh/ /media/usb/img/noe.img

During this operation, Scalpel really does scratch six files back off the disk. Because of the lack of footer information,
they are all exactly 70,000,000 bytes long and contain a lost “Sandmann” sequence – with a more or less large chunk of junk data at the end (Figure 3).
Cheers for dad!




"OGG encoding"
/** NOTE example*/
oggenc -a nintendo -t super_mario_world_coin -l super_mario_world -G game -c "nintendo rules!" -o smw_coin.ogg smw_coin.wav

"Rip a DVD with mencoder"

mencoder dvd:// -ovc lavc -lavcopts vcodec=mpeg4:vhq:vbitrate="1200" -vf scale -zoom -xy 640 -oac mp3lame -lameopts br=128 -o /disk/rips/mov.avi

######################################
"rdesktop"
rdesktop -a 16 -x 1 -g 1600x860 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -u Sistemastbk -p S1st3m5s5 192.168.20.235 -k es -r disk:sharename=/media/usb0/

"M Apache server "

This is very annoying error in cakephp when you get blank page. Usually, there is nowhere to look at possible problems, because even if debug level is 2 and error reporting is set to all - it just shows white blank page - nothing else. And in many cases the error.log file does not contain anything useful either.

This most probably because display_error is not enabled for apache server. To check if it is enabled or disabled move the cakephp files to another folder, create simple index.php and put <?php phpinfo();?> in it.  On the page search display_errors string: if it is off, then it is necessary to enable it. For that, add or uncomment(if exists) the following line in php.ini file. Important: This line should be in php.ini file, it won't work if try to add this in some files of the website.

ini_set('display_errors', 1);


add group
sudo addgroup developers

"Add user to /var/www"
Add a Linux User With Document Root Permissions
	sudo usermod -a -G www-data demo
Change the document root permissions
Set the group
	sudo chgrp -R www-data /var/www


"compress resize pdf"
gs -sDEVICE=pdfwrite -dCompatibilityLevel=1.4 -dPDFSETTINGS=/screen -dNOPAUSE -dQUIET -dBATCH -sOutputFile=output.pdf input.pdf

OR
gs -sDEVICE=pdfwrite -dCompatibilityLevel=1.4 -dDownsampleColorImages=true -dColorImageResolution=150 -dNOPAUSE  -dBATCH -sOutputFile=output.pdf input.pdf

bin/bin/shrinkpdf.sh

pdftk file1.pdf output file2.pdf compress

convert -density 200x200 -quality 60 -compress jpeg input.pdf output.pdf

this command works too for images formats

convert -density 200x200 -quality 60 -compress jpeg input.png output.jpg
"pdf with ps"

pdf2ps large.pdf very_large.ps
ps2pdf very_large.ps small.pdf

"pdf whith convert"
convert -density 200x200 -quality 60 -compress jpeg input.pdf output.pdf
	OR
this is test number 1
sent from linux box
.

Note : Do not forgot the "." at end which represents end of the message

"File Encoding Convert the utf16 to utf8 "
// While using vim to do it is perfectly possible, why don't you simply use iconv?
// I mean - loading text editor just to do encoding conversion
// seems like using too big hammer for too small nail.
// Just:

iconv -f utf-16 -t utf-8 file.xml > file.utf8.xml


#####################################################################################################################

/**
 * Samba section
 */
# for connect to a server samba
sudo mount --verbose -t cifs //192.168.20.235/Backup /media/smb/ -o username=Administrator,password=IntegrADB2014,domain=INTEGRADB,sec=ntlm


"To enable Windows netbios name resolution from a Linux computer"
    make sure that Samba is installed (although the smb service does not need to be running). The Samba suite includes winbind, which enables Windows host names to be resolved. Then edit /etc/nsswitch.conf and change this line:

    hosts:      files dns

    to this:

    hosts:      files dns wins

    Then test by pinging the computer name of Windows machine on the LAN:

    $ ping windowsbox

"add users from command line"

net user username userpasswd /ADD /PASSWORDCHG:NO
example
net user John fadf24as /ADD /DOMAIN

To add a domain user to local administrator group:

net localgroup administrators domainname\username /add

To add a user to remote desktop users group:

net localgroup "Remote Desktop Users" UserLoginName  /add

"add a remote samba share"

net use s: \\tower\movies /P:Yes
/P = persistent mode
net use s: \\tower\movies /user:HTG Pa$$word
 for delete resource
 net use s: /delete

"How to Use 12VoIP with your SIP device"

http://www.12voip.com/en/sipp.html

Software configuration
General
	SIP port : 5060
	Registrar : sip.12voip.com
	Proxy server : sip.12voip.com
	Outbound proxy server : leave empty
	Account name : your 12VoIP username
	Password : your 12VoIP password
	Display name/number : your 12VoIP username or voipnumber
	Stunserver (option) : stun.12voip.com
Codecs
	G.711 (64 kbps)
	G.726 (32 kbps)
	G.729 (8 kbps)
	G.723 (5.3 & 6.3 kbps)
	GSMFR (13.2 kbps)
If you have audio problems:
Use a STUN server (e.g. stun.12voip.com) with port 3478 (if supported by your device)
Use the G.711 codec
/*USD Dollars
					 2012
Mexico  (Landline)	0.100	  0.116  0.0810
Mexico  (Mobile)	0.107	  0.123  0.0860
Mexico [guadalajara]   	FREE*	  FREE*
Mexico [mexico City]   	FREE*	  FREE*
Mexico [monterrey]   	0.043	  0.050
SMS
Mexico  	0.043	  0.050
FREE ext
Mexico [guadalajara]   	0.014	  0.017
Mexico [mexico City]   	0.014	  0.017
*/
//ambagasdowa@gmail.com:Alexa001011
All 12VoIP calls are subject to a 0.039 EURO connection fee aprox 0.052 USD.

" Find the serial number in a windows machine "
wmic bios get serialnumber


"Prehacking Section"

To send SMS:
 echo "message" | gammu sendsms TEXT number

gammu backup file.vcf -yes

etc ....
 for more info check gammu --help

"Public DNS"
[opendns]
nameserver 208.67.222.222
nameserver 208.67.220.220
[google dns]
nameserver 8.8.8.8
nameserver 8.8.4.4
[telmex]
200.33.146.201
200.33.146.202

If you need to manually define the nameservers (as with a static inferface), add a line
something like the following to the interfaces configuration file at /etc/network/interfaces:

dns-nameservers 12.34.56.78 12.34.56.79

" Making an extension with utp to usb "

white orange & orange to red, Vcc
white & green to white, D-
green to green, D+
and the rest to black. Gnd

up to 20 mts and someone says that works on 50 ft too

#############################################################################################
 " installing audio drivers on compaq v3500 (V3617LA)"

Download ConexantHDAudio 221_XP
Go to ControlPanel->System->Hardware->DeviceManager
Select your audio device marked with '?'.
Select Details.
Now yo can see something like:
AUDIO\VEN_14F1&DEV_5051&SUBSYS_103C30D9
14F1 & 5051 strins are "Conexant cx20561" or "Conexant HD-Audio SmartAudio 221" identification. Add the string 103C30D9 in .inf file located in
ConexantHDAudio 221_XP\XP32\WiSVHe5.inf
in a line like:
%HdAudioFunctionDriver.Hermosa5051.DeviceDesc% = HdAudModel,HDAUDIO\FUNC_01&VEN_14F1&DEV_5051&SUBSY S_103C30D9
Save and install the driver. Now the installer will recognize your hardware.
Gool look!!

	Stunserver (option) : stun.12voip.com
Codecs
	G.711 (64 kbps)
	G.726 (32 kbps)
	G.729 (8 kbps)
	G.723 (5.3 & 6.3 kbps)
	GSMFR (13.2 kbps)
If you have audio problems:
Use a STUN server (e.g. stun.12voip.com) with port 3478 (if supported by your device)
Use the G.711 codec
/*USD Dollars
					 2012
Mexico  (Landline)	0.100	  0.116  0.0810
Mexico  (Mobile)	0.107	  0.123  0.0860
Mexico [guadalajara]   	FREE*	  FREE*
Mexico [mexico City]   	FREE*	  FREE*
Mexico [monterrey]   	0.043	  0.050
SMS
Mexico  	0.043	  0.050
FREE ext
Mexico [guadalajara]   	0.014	  0.017
Mexico [mexico City]   	0.014	  0.017
*/
//ambagasdowa@gmail.com:Alexa001011
All 12VoIP calls are subject to a 0.039 EURO connection fee aprox 0.052 USD.

" Find the serial number in a windows machine "
wmic bios get serialnumber


"Prehacking Section"

To send SMS:
 echo "message" | gammu sendsms TEXT number

gammu backup file.vcf -yes

etc ....
 for more info check gammu --help

"Public DNS"
[opendns]
nameserver 208.67.222.222
nameserver 208.67.220.220
[google dns]
nameserver 8.8.8.8
nameserver 8.8.4.4
[telmex]
200.33.146.201
200.33.146.202

If you need to manually define the nameservers (as with a static inferface), add a line
something like the following to the interfaces configuration file at /etc/network/interfaces:

dns-nameservers 12.34.56.78 12.34.56.79

" Making an extension with utp to usb "

white orange & orange to red, Vcc
white & green to white, D-
green to green, D+
and the rest to black. Gnd

up to 20 mts and someone says that works on 50 ft too

#############################################################################################
 " installing audio drivers on compaq v3500 (V3617LA)"

Download ConexantHDAudio 221_XP
Go to ControlPanel->System->Hardware->DeviceManager
Select your audio device marked with '?'.
Select Details.
Now yo can see something like:
AUDIO\VEN_14F1&DEV_5051&SUBSYS_103C30D9
14F1 & 5051 strins are "Conexant cx20561" or "Conexant HD-Audio SmartAudio 221" identification. Add the string 103C30D9 in .inf file located in
ConexantHDAudio 221_XP\XP32\WiSVHe5.inf
in a line like:
%HdAudioFunctionDriver.Hermosa5051.DeviceDesc% = HdAudModel,HDAUDIO\FUNC_01&VEN_14F1&DEV_5051&SUBSY S_103C30D9
Save and install the driver. Now the installer will recognize your hardware.
Gool look!!

appears that 103C0D6 is for LA models


"############# download chrome   ##################"

Descarga Google Chrome 18.0.1025.151 aquí (Instalador OnLine)
- http://www.google.com/chrome?hl=es

Descarga Google Chrome 18.0.1025.151 aquí (Instalador OffLine)
- http://www.google.com/chrome/eula.html?standalone=1


"Intall Fonts TTF ttf"

Just create a .fonts folder in your home directory (if it doesn't exist) and unpack/copy them there.
It's also a good idea to manually rebuild the font cache, so you can immediately use the fonts everywhere:
fc-cache -f -v


"######### tips for tty ######"
"redirecting output"
./script 2>&1 | tee logfile.txt



"remove files"

Linux Shell Tip: Remove files with names that contains spaces, and special characters such as -, --

In Linux or Unix-like system you may come across file names with special characters such as:

    -
    --
    ;
    &
    $
    ?
    *
    White spaces, backslashes and more.

In this quick tip I am going to show you to delete or copy files with names that contain strange characters on Linux.
Sample file list

Here is a sample list of file names:

file-1
The problem and solution

Your default bash shell considers many of these special characters (also known as meta-characters) as commands. If you try to delete or move/copy such files you may end up with errors. In this example, I am trying to delete a file named '>file':

$ rm >file

Sample outputs:

rm: missing operand
Try `rm --help' for more information.

The rm command failed to delete the file due to strange character in filename.
Tip #1: Put filenames in quotes

The following command is required to copy or delete files with spaces in their name, for example:

$ cp "my resume.doc" /secure/location/
$ rm "my resume.doc"

The quotes also prevent the many special characters interpreted by your shell, for example:

$ rm -v ">file"
removed `>file'

The double quotes preserve the value of all characters enclosed, except for the dollar sign, the backticks and the backslash. You can also try single quotes as follows:

$ rm -v 'a long file   name  here'
$ cp 'my mp3 file.mp3' /backup/disk/

Tip #2: Try a backslash

You can always insert a backslash (\) before the special character in your filename:

$ cp "my\ resume.doc" /secure/location/
$ rm "\*file"

Tip #3: Try a ./ at the beginning of the filename

The syntax is as follows to delete a file called '-file':

$ rm -v ./-file
removed `./-file'

The ./ at the beginning of the filename forces rm not to interpret - as option to the rm command.
Tip #4: Try a -- at the beginning of the filename

A -- signals the end of options and disables further option processing by shell. Any arguments after the -- are treated as filenames and arguments. An argument of - is equivalent to --. The syntax is:

$ rm -v -- -file
$ rm -v -- --file
$ rm -v -- "@#$%^&file"
$ rmdir -v -- "--dirnameHere"

Tip #5: Remove file by an inode number

The -i option to ls displays the index number (inode) of each file:

ls -li

Use find command as follows to delete the file if the file has inode number 4063242:

$ find . -inum 4063242 -delete

OR

$ find . -inum 4063242 -exec rm -i {} \;

find /etc -type f -mtime -1 -exec cp '{}' /tmp/modificados/ \;

find /etc -type f -mtime 0 -exec ls -gGh --full-time '{}' \; | cut -d ' ' -f 4,5,7

Si quisiéramos además ignorar los archivos ocultos lo hacemos con:

1 find \( ! -path './.*' \) -mtime +1 -mtime -15 -ls

find \( ! -path './.*' \) -mtime +2 -mtime -65 -exec rm -i {} \; # search and delete


Sample session:file-2

For more information and options about the find, rm, and bash command featured in this tip, type the following command at the Linux prompt, to read man pages:

$ man find
$ man rm
$ man bash

"SPLIT A FILE"
How to split one text file into multiple *.txt files

$ split -l 100 input_file output_file

where -l is the number of lines in each files. This will create:

    output_fileaa
    output_fileab
    output_fileac
    output_filead

"format for load soundcloud url"
-- http://soundcloud.com/saleha123x/sets/oldies
ex: mpc load soundcloud://url/soundcloud.com/saleha123x/sets/oldies

"printing"

lp -P 1,2-10,12,15,30  -o page-set=[odd/even] -o landscape -o media=letter file.ext

also for -P [page-list] use -o page-ranges=[ranges]
for cancel a job
lpstat for id and
cancel job-id or lprm job-id
odd=impar , even = par
lpoptions -l to see options of the default printer

// watch printers
lpstat -p

// cmd output example
cal 2017 | lp -d PDF -o fit-to-page -o media=letter

" format a device with bad blocks  "

badblocks -o file_with_badblocks /dev/sdX
mkfs.vfat -l file_with_badblocks /dev/sdX

# on this way the badblocks are skipped

"Change Desktop resolution from console "

use xrandr -s N
(N is the id list of resolutions avaliable from exec randr command)
/* Activate the vga output from a lap for example */
  xrandr --output VGA1 --auto --left-of eDP1 isn't obvious!!
  powercfg /h off for shutdown the hibernate option in windows

 "Two Sound cards"
example with mplayer

mplayer -ao oss:/dev/dsp1 file.ext
mplayer -ao alsa:device=hw=1.0 file.ext

"Disassembler sections"

hexdump of course but find a new
strings pe-exe-file.exe
nm pe-exe-file.exe
objdump -s pe-exe-file.exe fetch the strings ussefull for walware
objdump -x -D pe-exe-file.exe | less for a full info

 and
 boomerang  /*http://boomerang.sourceforge.net/download.php*/
 REC /*http://www.backerstreet.com/rec/rec.htm*/

"file for change some mime-type"

~/.mailcap

prog : xdg-mime

"Convert mkv to avi with ffmpeg"

ffmpeg -i input_video.mkv -vcodec mpeg4 -b 4000k -acodec mp2 -ab 320k converted_output.avi

and for

You can use the -map option (full documentation) to select specific input streams and map them to your output.

The most simple map syntax you can use is -map i:s, where i is the input file ID and s is the stream ID, both starting with 0. In your case, that means we select track 0 and 4:

ffmpeg -i vidui.mkv -c:v libx264 -c:a ac3 -crf 20 -map 0:0 -map 0:4 vidui.mp4

If you want to choose video, audio or subtitle tracks specifically, you can also use stream specifiers:

ffmpeg -i vidui.mkv -c:v libx264 -c:a ac3 -crf 20 -map 0:v:0 -map 0:a:1 vidui.mp4

Here, 0:v:0 is the first video stream and 0:a:1 is the second audio stream.

	or
mencoder input.mpg -o output.avi -ovc lavc -lavcopts vcodec=mpeg4:vbitrate=5000 -oac mp3lame -lameopts mode=2:cbr:br=128:vol=5
    or
mencoder video.mkv -ovc xvid -oac mp3lame -oac mp3lame -lameopts abr:br=192 -xvidencopts pass=2:bitrate=-700000 -o output.avi


"show image properties"

identify -verbose file.[jpg|png|etc]
identify -ping file.[jpg|png|etc]

"resize an image"
convert -resize NumxNum file.img
convert file.jpg -resize [XxY|n%] file.png

"burn files directly to media"
To master and burn an ISO9660 volume with Joliet and Rock-Ridge  exten-
       sions on a DVD:

            growisofs -Z /dev/dvd -R -J /some/files

To append more data to same DVD:

            growisofs -M /dev/dvd -R -J /more/files

To use growisofs to write a pre-mastered ISO-image to a DVD:

            growisofs -dvd-compat -Z /dev/dvd=image.iso

The tricky part is getting the dimensions right. I will give two examples.
First, the original scans were 180 dpi 24-bit color, letter size. The following command creates a
letter size 120 DPI color PDF. Adjust the dimensions after -resize and -density for different size
 originals and different PDF resolutions as needed. For example, for 150 DPI use 1275x1650 and
150x150 respectively.

 $> convert scan.png -resize 1020x1320 -units PixelsPerInch -density 120x120 scan.pdf

The following command converts a series of 120 DPI 24-bit color scans (named scan001.png, scan002.png, etc.) to a single 120 DPI 256-color grayscale PDF:

 $> convert scan*.png -compress zip -colors 256 -colorspace GRAY -resize 1020x1320 -density 120x120 -units PixelsPerInch output.pdf


"Ok and for the pdf edition"
set inkscape as a editor
import pdf to edit > edit
if need clip cut a section of a pdf
1-Choose the Rectangle Tool from the Toolbar
2-select all objects > ctrl-A
3-then go to Object->clip->set
4- copy , edit or anything
5-at last merge into your pdf ed needs
fot join all the resulting pdf use
pdfjoin fileToJoin1.pdf fileToJoin2.pdf fileToJoinn.pdf <files.pdf>
for keep the quality
enjoy!

## join pdf
gs -dBATCH -dNOPAUSE -q -sDEVICE=pdfwrite -dPDFSETTINGS=/prepress -sOutputFile=merged.pdf mine1.pdf mine2.pdf
or
pdfjoin a.pdf b.pdf this output a joined pdf

"Taking screenshots with console"

# for tty
scrot --delay N file.png
# for X
import -window root filename.ext


"sysprep"
for dual core
update the files in system32/
Hal.dll, ntkrnnlpa.exe and ntoskrnl.exe

edit sysprep.inf >
UpdateHal=ACPIAPIC_MP, %windir%\inf\hal.inf

//maybe %windir% can be caps

"keyboard mapping utilities"
//Emulators config
for console
showkey -k
*************************
xev
xmodmap
*************************
"Extracting several zip files with escape characters"

find ./path/to/file(s).zip -iname *.zip -type f -print -exec unzip -d /dir/to/extract '{}' \;

"Count files in current directory"
ls -1 | wc -l

"forward display"

Has usado la consola para hcerla hablar a la maquina eyebrows eyebrows esa esta buena eyebrows
# apt-get install alsa-oss espeak espeak-data
Luego para hacer hablar a la consola (pero local no remota), es de la siguiente forma eyebrows
aoss espeak -ves -p90 -s100  “que,estas,haciendo,en,la,maquina”
aoss espeak -ven -p10 -s120 “my,tailor,is,rich,and,my,mother,is,in,the,kitchen”

gwenview -f flores.jpg -display :0

"converter of office formats includes ms-office"

soffice -headless -accept="socket,port=8100;urp;" ;
jodconverter file_input file_output ;
killall soffice.bin ;

"mod firefox "
add
privacy.trackingprotection.enabled ; true
for improve the speed


"hide bash-history"

for hidde the bash-history type :

HIST_FILE=/dev/null
OR "export HISTSIZE=0"
aptitude install snoopy

"see the mac of a external net interace"

/usr/sbin/arp <ip-address>

"For see the UUID"
use blkid as root

"install all normalized package in one shoot"
You can get a list of installable fonts by searching for packages beginning with fonts- and xfonts-.

apt-cache search fonts | grep '^xfonts-'

Now you can install what you want

"sudo apt-get install -y xfonts-..."

mount as UUID the usb mass storage dev Do this with all USB device and all will have a static name


"Alcatel One Touch"

How to take a screenshot with the Alcatel One Touch

Alcatel One TouchHold down the POWER and VOLUME DOWN key at the same time.


"Use of aspell"
aspell -c -x --lang=es file
aspell -c -x --mode=[ccpp|html|none] file
see man aspell for details

"Mounting Crypted filesystem"

 - First up - boot from desktop liveCD and make sure you have an internet
    connection.
 - Now you have to install some stuff thats not there in the liveCD. Pull up a
   terminal (Applications>Accessories>Terminal)
 - type "sudo su"
 - type "apt-get install lvm2 cryptsetup
 - type "mkdir /media/test"
 - type "modprobe dm-crypt"
 - type "cryptsetup luksOpen /dev/sda5 test"
   (enter password... are you set up on sda3 or sda5? change as needed)
   (you should get a command successful message... continue●
 - type "vgchange -ay"
   (this will reveal your volume group name)
 - type "mount /dev/("volume group name")/home /media/test
 - type "nautilus /media/test"
   (and now your encrypted /home should open in a window.)
 If all that works... manually mounting the partition... then its not the encrypted
 header or partition thats got a problem. So close it all up...
 - close nautilus
 - type "umount /media/test"
 - type "cryptsetup luksClose /dev/sda5

"install x cursors theme"

cp Aqua-icons/cursors/ dir to /usr/share/icons/
then edit the file
/usr/share/icons/default/index.theme
[Icon Theme]
inherits=Aqua-icons

Note:  If the animations are flickering on your nvidia card, add the following line to your
/etc/X11/xorg.conf file, into the nvidia device section, to fix it:

Option "HWCursor" "off"

You can change the shape of the mouse cursor when it is above the root window (this doesn't change
the cursor when it is above other windows) using the following command:
   " xsetroot -cursor_name <cursorname>"
<Cursorname> is one of the ``standard'' X Windows mouse cursor shapes. Here is a list of some interesting ones to try out:
"    xsetroot -cursor_name draped_box          "
"    xsetroot -cursor_name hand1               "
"    xsetroot -cursor_name hand2               "
"    xsetroot -cursor_name iron_cross          "
"    xsetroot -cursor_name left_ptr            "
"    xsetroot -cursor_name plus                "
"    xsetroot -cursor_name top_left_arrow      "
"    xsetroot -cursor_name watch               "

"change the gtk-theme"
use gtk-chtheme

"utils for msoffice "
sc - Text-based spreadsheet with VI-like keybindings
cone - COnsole Newsreader and Emailer
catdoc - MS-Word to TeX or plain text converter
xlhtml - convert xls to html

"convert pdf to image"

convert doc.pdf doc%4d.jpg >> output as doc0000.jpg

convert -geometry 1440x1440 -density 200x200 -quality 100 doc.pdf doc.png

convert doc-0.png documento-1.png doc_nuevo.pdf

"About this"
“DISPLAY=:0 dcop amarok player next”
"Music"
streamtuner , tunapie
"rip music from streaming"
streamripper `mpc --format %file% | grep 'http'` -d /tmp/

"Using Git"
example visit github.com

git clone git://github.com/MisterTea/ClientServerMAME.git

// create a new repository on the command line

echo "# sql" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:ambagasdowa/sql.git
git push -u origin master

// push an existing repository from the command line

git remote add origin git@github.com:ambagasdowa/sql.git
git push -u origin master

// from cyberia

git add .
git commit -m "un mensaje X"
git push origin master
y si te marca error que no lo creo
le das
git pull origin master

// API from Google maps
AIzaSyAgUrZtQOSez0eloLKSlNoRXVP9MCQYrIM //2017-03-02



"Elimina líneas duplicadas de un archivo"
cat pass.txt | sort | uniq > pass-listos.txt

"for see the UUID "
run "blkid" command

"Refresh font cache"
install fonts on /usr/share/fonts/truetype  then
fc-cache -f -v

dpkg -l | egrep "^ii .*(xen|vnc|sdl)" | awk '{print $2}'
tri it for an example

awk '/MemTotal/ {print $1" "$2" "$3}' /proc/meminfo

sudo apt-get --reinstall install $(dpkg -l `apt p python` 2>/dev/null
> | awk '{if ($1 == "ii") print $2}')

aptitude -t testing install $(aptitude search ttf 2>/dev/null | awk '{if ($1 == "p") print
$2}')

"purgar paquetes desinstalados"
for i in $(dpkg -l |grep ^r| awk '{print $2}'); do aptitude purge -y $i; done

"Search and replace in vim"

#search a pattern an delete the rest of the line

%s/.\{-}\(\S\+@\S\+\).*/\1/g

\S\+@\S\+ => this is for Emailer

#This will find everything but the regular expression you have specified. For example, if we want to find all the lines not containing the word 'foo', simply do:

/^\(\(.*foo.*\)\@!.\)*$

#and for remove just not email lines

:g /^\(\(.*\S\+@\S\+.*\)\@!.\)*$/d

"vim replace vertical to horizontal"

replace this

 1000716
 1000713
 1000714

into this
'1000716', '1000713', '1000714'


:%s/^/'/g | %s/$/',/g | %j


The :substitute command searches for a text pattern, and replaces it with a text string. There are many options, but these are what you probably want:
:%s/foo/bar/g
Find each occurrence of 'foo', and replace it with 'bar'.
:%s/foo/bar/gc
Change each 'foo' to 'bar', but ask for confirmation first.
:%s/\<foo\>/bar/gc
Change only whole words exactly matching 'foo' to 'bar'; ask for confirmation.
:%s/foo/bar/gci
Change each 'foo' (case insensitive) to 'bar'; ask for confirmation.
This may be wanted after using :set noignorecase to make searches case sensitive (the default).
:%s/foo/bar/gcI
Change each 'foo' (case sensitive) to 'bar'; ask for confirmation.
This may be wanted after using :set ignorecase to make searches case insensitive.
The g flag means global – each occurrence in the line is changed, rather than just the first.

Search range:
:s/foo/bar/g	 Change each 'foo' to 'bar' in the current line.
:%s/foo/bar/g	 Change each 'foo' to 'bar' in all lines.
:5,12s/foo/bar/g	 Change each 'foo' to 'bar' for all lines from line 5 to line 12 inclusive.
:'a,'bs/foo/bar/g	 Change each 'foo' to 'bar' for all lines from mark a to mark b inclusive.
:.,$s/foo/bar/g	 Change each 'foo' to 'bar' for all lines from the current line (.) to the last line ($) inclusive.
:.,+2s/foo/bar/g	 Change each 'foo' to 'bar' for the current line (.) and the two next lines (+2).
:%s/foo/bar/g	 Equivalent to :1,$s/foo/bar/g (change all lines).
:g/^baz/s/foo/bar/g	 Change each 'foo' to 'bar' in each line starting with 'baz'.
When searching:
., *, \, [, ], ^, and $ are metacharacters.
+, ?, |, {, }, (, and ) must be escaped to use their special function.
\/ is / (use backslash + forward slash to search for forward slash)
\t is tab, \s is whitespace
\n is newline, \r is CR (carriage return = Ctrl-M = ^M)
\{#\} is used for repetition. /foo.\{2\} will match foo and the two following characters. The \ is not required on the closing } so /foo.\{2} will do the same thing.
\(foo\) makes a backreference to foo. Parenthesis without escapes are literally matched. Here the \ is required for the closing \).
When replacing:
\r is newline, \n is a null byte (0x00).
\& is ampersand (& is the text that matches the search pattern).
\1 inserts the text of the first backreference. \2 inserts the second backreference, and so on.
You can use other delimiters with substitute:
:s#http://www.example.com/index.html#http://example.com/#
Save typing by using \zs and \ze to set the start and end of a pattern. For example, instead of:
:s/Copyright 2007 All Rights Reserved/Copyright 2008 All Rights Reserved/
Use:
:s/Copyright \zs2007\ze All Rights Reserved/2008/
Using the current word or registersEdit
:%s//bar/g
Replace each match of the last search pattern with 'bar'.
For example, you might first place the cursor on the word foo then press * to search for that word.
The above substitute would then change all words exactly matching 'foo' to 'bar'.
:%s/foo/<c-r><c-w>/g
Replace each occurrence of 'foo' with the word under the cursor.
<c-r><c-w> means that you press Ctrl-R then Ctrl-W.
The word under the cursor will be inserted as though you typed it.
:%s/foo/<c-r><c-a>/g
Replace each occurrence of 'foo' with the WORD under the cursor (delimited by whitespace).
<c-r><c-a> means that you press Ctrl-R then Ctrl-A.
The WORD under the cursor will be inserted as though you typed it.
:%s/foo/<c-r>a/g
Replace each occurrence of 'foo' with the contents of register 'a'.
<c-r>a means that you press Ctrl-R then a.
The contents of register 'a' will be inserted as though you typed it.
:%s/foo/\=@a/g
Replace each occurrence of 'foo' with the contents of register 'a'.
\=@a is a reference to register 'a'.
The contents of register 'a' is not shown in the command. This is useful if the register contains many lines of text.
:%s//<c-r>//g
Replace each match of the last search pattern with the / register (the last search pattern).
After pressing Ctrl-R then / to insert the last search pattern (and before pressing Enter to perform the command), you could edit the text to make any required change.
:%s/<c-r>*/bar/g
Replace all occurrences of the text in the system clipboard (in the * register) with 'bar' (see next example if multiline).
On some systems, selecting text (in Vim or another application) is all that is required to place that text in the * register.
:%s/<c-r>a/bar/g
Replace all occurrences of the text in register 'a' with 'bar'.
<c-r>a means that you press Ctrl-R then a. The contents of register 'a' will be inserted as though you typed it.
Any newlines in register 'a' are inserted as ^M and are not found.
The search works if each ^M is manually replaced with '\n' (two characters: backslash, 'n').
This replacement can be performed while you type the command:
:%s/<c-r>=substitute(@a,"\n",'\\n','g')<CR>/bar/g
The "\n" (double quotes) represents the single character newline; the '\\n' (single quotes) represents two backslashes followed by 'n'.
The substitute() function is evaluated by the <c-r>= (Ctrl-R =) expression register; it replaces each newline with a single backslash followed by 'n'.
The <CR> indicates that you press Enter to finish the = expression.

Additional examplesEdit
:%s/foo/bar/
On each line, replace the first occurrence of "foo" with "bar".
:%s/.*\zsfoo/bar/
On each line, replace the last occurrence of "foo" with "bar".
:%s/\<foo\>.*//
On each line, delete the whole word "foo" and all following text (to end of line).
:%s/\<foo\>.\{5}//
On each line, delete the whole word "foo" and the following five characters.
:%s/\<foo\>\zs.*//
On each line, delete all text following the whole word "foo" (to end of line).
:%s/.*\<foo\>//
On each line, delete the whole word "foo" and all preceding text (from beginning of line).
:%s/.*\ze\<foo\>//
On each line, delete all the text preceding the whole word "foo" (from beginning of line).
:%s/.*\(\<foo\>\).*/\1/
On each line, delete all the text preceding and following the whole word "foo".
:s/^\(\w\)/\u\1/
If the first character at the begining of the current line is lowercase, switch it to uppercase using \u (see switching case of characters).
:%s/\(.*\n\)\{5\}/&\r/
Insert a blank line every 5 lines. The pattern searches for \(.*\n\) (any line including its line ending) repeated five times (\{5\}). The replacement is & (the text that was found), followed by \r (newline).
Special casesEdit
For substituting patterns with a corresponding case-sensitive text, Michael Geddes's keepcase plugin can be used, e.g.:
:%SubstituteCase/\cHello/goodBye/g
Substitute 'Hello hello helLo HELLO' by 'Goodbye goodbye goodBye GOODBYE'
For changing the offsets in a patch file (line number of a block), this little snippet can be used:
s/^@@ -\(\d\+\),\(\d\+\) +\(\d\+\),\(\d\+\) @@$/\="@@ -".eval(submatch(1)+offsetdiff).",".submatch(2)." +".eval(submatch(3)+offsetdiff).",".submatch(4)." @@"/g
Useful when we want to strip some blocks from a patch, without patch having to complain about offset differences.
Note Should try to make the expression more compact, but don't know how without having the possibility of modifying unwanted lines.



### "For replace a numeral line ex" ###

 1. - http=code_etc
 2. - http=code_etc2
 ...
 25. - http=code_etc25

TO
http=code_etc
...
http=code_etc25

do
 %s /\ \([0-9][0-9]\)\ -\ //g
for lines 10-99
and then
 %s /\ \([0-9]\)\ -\ //g

for lines 1-9
done

"Adjust the brightness and contrast with a command"

xgamma -gamma f.f

useful for fim viewer
:popen "xgamma -gamma 2" /* For adjust the darkest pics*/

"Settings for Display and the gamma"
# Extends monitor
xrandr --output VGA1 --auto --primary --output eDP1 --auto --right-of VGA1
#Set gamma for diferent screen
to see which monitors you have
xrandr -q

xrandr --output VGA1 --gamma 0.6:0.6:0.7 /* this set for monitor attached in VGA adaptor */
xrandr --output eDP1 --gamma 0.6:0.6:0.7 /* this set for monitor in the laptop*/

see man xranr for more options

and can use
:alias "light" 'popen "command -option 1 -option 2"';

can adjust the rgb channel to with rgamma ... etc options

"rename files"

rename 's/ string search that you want to replace / string to do replace /' *.ext

"the sed command"

sed 's/string_to_search/string_to_change/g' file

for a ranges by a line number

sed '1,100 s/../../g' file # for the firts 100 lines

for loop example: Cap {1..30}: http://url

sed 's/Cap [0-9]*: //g' file

the exit drops "Cap 1: " ... "Cap 2: " ... etc  in each line

"Example to upload files to Megaupload servers"
  /*#cd /media/books/emmerick
  #     listing emmerick*.pdf
  # can use for i in * ; ... etc or in a sequence -- for i in emmerick{1..n};
 */
  for i in $(ls emmerick*) ;
     do plowup megaupload -a ambagasdowa:001011 -d "Anne Catherine Emmerick-$i" $i ;
  done >> ./Emmerick_MU_links

 // #mplayer example
 // #better this with file cmd
  mplayer -vo gl -fs -softvol -volume 15 -subcp iso-8859-1 $(ls -u /tmp/Flash*)

"ftp issues"
 //same as sshfs
curlftpfs user:pass@ftp.host.com:/dir/in/ftp.server /path/to/mount -o umask=0,allow_other

for download a subdirs from ftp-server under dir/

wget -r --limit-rate=nk -c ftp://user:pass@ftp.host.com/dir/*
a better perfomance
wget -m ftp://user:pass@ftp.host.com/dir/*

for download
wget -m dir/
for upload
wput dir/ ftp://user:pass@host/path/to/dir


 // for upload a dir and subdirs

wput file url

echo "ftp://user:pass@ftp.host.com/path/to/dir/" | wput -i - myfile.ext ;

 //The simplest way to access a ftp server with username and password
curl ftp://myftpsite.com --user myname:mypassword

 //To download a file from ftp server
curl ftp://myftpsite.com/mp3/mozart_piano_sonata.zip --user myname:mypassword -o mozart_piano_sonata.zip

 //To upload a file to ftp server
curl -T koc_dance.mp3 ftp://myftpsite.com/mp3/ --user myname:mypassword

 //To list files in sub directories.
curl ftp://myftpsite.com/mp3/  --user myname:mypassword

 //List only directories, silent the curl progress bar, and use grep to filter
curl ftp://myftpsite.com  --user myname:mypassword -s | grep ^d

 //Remove files from ftp server.
curl ftp://myftpsite.com/ -X 'DELE mp3/koc_dance.mp3' --user myname:mypassword

"ls xbox-saves"
./xsave.pl -l

 "Commands for unleashx"
 site reboot, shutdown, powercycle, trayclose , eject or trayopen , ftpreset, netreset


"For make Headers"

banner "text" or figlet "text"

"set up bandwidth with postgresql"
Thanks a lot for the tips in the posts above!

After a long fight if got bandwidthd with postgre database and PHP running under ubuntu 11.04.

Here my steps to got it working:
--------------------------------

1.) install packets
#> aptitude install postgresql-8.4
#> aptitude install apache2.2-bin libapache2-mod-php5
#> aptitude install php-mdb2-driver-pgsql
#> aptitude install bandwidthd-pgsql

1a.) during installation of bandwidthd-pgsql some setting will be asked, i answered like this
- pgsql connection type: socket
- authentication method: password (ident will not work here)
- Database Name: bwddb
- Database User: bwduser
- Database Password: bwdpass
- Database Admin: postgres

2.) create database and user role
#> createdb -U postgres -E utf-8 bwddb
#> psql -U postgres
postgres=# CREATE ROLE bwduser with CREATEDB LOGIN PASSWORD 'bwdpass';
postgres=# \q

3.) to let PHP access the database the connection parameters needed, edit /etc/bandwidthd/debian-db.php like this:
#> vi /etc/bandwidthd/debian-db.php
$dbuser='bwduser';
$dbpass='bwdpass';
$basepath='';
$dbname='bwddb';
$dbserver='localhost';
$dbport='';
$dbtype='pgsql';

4.) actually (Sep 2011) index.php is missing, create a ling (as mentioned above)
#> ln -s /var/lib/bandwidthd/htdocs/sensors.php index.php

5.) On problems with db access adjust postgre configuration:
#> vi /etc/postgresql/8.4/main/pg_hba.conf

local all postgres trust
# TYPE DATABASE USER CIDR-ADDRESS METHOD
# "local" is for Unix domain socket connections only
local all all trust
# IPv4 local connections:
host all all 127.0.0.1/32 trust
# IPv6 local connections:
host all all ::1/128 trust

6.) restart the services
#> /etc/init.d/apache2 restart
#> /etc/init.d/bandwidthd restart
#> /etc/init.d/postgresql restart

7.) analyse of problems
a.) youre /etc/bandwidthd/bandwidthd.conf should somehow look like this:
root@gate:~> cat /etc/bandwidthd/bandwidthd.conf | grep -v "^#" | grep -v "^$"
subnet 10.0.1.1/32
subnet 10.0.1.10/32
subnet 134.165.3.0/24
subnet 192.168.200.0/24
subnet 192.168.210.0/24
dev "eth0"
pgsql_connect_string "user = bwduser password = bwdpass dbname = bwddb host = localhost"
sensor_id "gate"
graph false
recover_cdf false
promiscuous false

(subnet and dev adjusted to your fit system)

b.) check /var/log/apache2/error.log for errors


Best Regards


//***********************    HDD gurus     ***********************//

"grow up a partition"
 delete the partition to use and re-create to use entire space

then run ntfsresize -n /dev/hdxn
x=a,b,c,d : n 1-4 for primary part and 5-n for logical

"Resize the partition"

firts run ntfsrezise for shrink the size of the filesystem then you can use fdisk for shrink
the size of the partition and recreated with a smaller size with the same spec
 example use ntfsresize --size 10G /dev/hda1
then delete and recreate the partition with fdisk using 10G as parameter
and u (sector) flag instead of cilinder
remenber the a parameter for bootable partition
and of course ms-sys -m /dev/sdx for win XP master boot record


"instalar varios paquetes listados en un archivo"
for i in $(cat list_of_pkgs); do aptitude -t testing install $i; done

for i in $(cat fonts2); do aptitude -t testing install $i; done

dd or cp harddrive over the LAN
I would use rsync, as it allows compression

rsync -aAXxPz / root@178.63.65.136:/
where -a preserves permissions, modification times etc
-A preserves acls
-X preserves xattrs
-x keeps you in one filesystem (so you won't copy /dev etc.)
-P keeps partial copied files for faster stop-and-proceed, and shows a progress bar
-z compresses the data stream

This should give you an identical copy. Good luck!

I use -HaAxX (mnemonic: "hacks") when archiving over rsync.  That preserves
everything (hard/symlinks, ACLs, permissions, extended attributes, etc.).

You can use dd via ssh if you wish. I think the command would be:
dd if=/dev/sda1 | ssh user@host "dd of=/path/to/sda1.img"

netcat may help:
dd if=/dev/sdX | nc -l 4321
On the other box:
nc ip.of.the.serverbox 4321 | dd of=/dev/sdY

// remove ofending host from ssh
"ssh-keygen -R hostname"

// Directly ssh to host B that is only accesible through host A

"ssh -t host_A ssh host_B"

//How to run X without any graphics hardware

"startx -- `which Xvfb` :1 -screen 0 800x600x24 && DISPLAY=:1 x11vnc"

###################################################################################################
rtorrent: tracker group list is not a list

It seems like some torrents contain malformed announce-lists making clients which use libtorrent like rtorrent to reject torrent though it is downloadable.
To fix this, either upgrade libtorrent to the latest version or use a hex editor and replace "announce-list" with "xnnounce-list" in the .torrent file.
##################################################################################################
If you have X running on some display, you can copy/paste to/from ttys using xclip.
On the tty, run
export DISPLAY=:0.0
(replacing :0.0 with the running X display).
You can work with the X clipboard normally in a GUI environment (highlight/middle-click), and in a terminal emulator or on a tty, use xclip -i to place something in the clipboard and xclip -o to retrieve the clipboard contents.

using ctrl+k cut forward
ctrl+u cut backwards
ctrl+w cut backwards to firts " " space
to paste use ctrl+y

#!!comando para escuchar tu disco duro
cat /dev/sda | aplay -fdat


/**For cheking speaker examples
*/
speaker-test -Dplug:surround51 -c6 -l5 -twav
speaker-test -Dplug:front -c2 -l5 -twav

/**
Good catch w/ the MS adapter, I put a multimeter to it and it really seems to be just a point-to-point connector. To clear that up, we are talking about the MS SideWinder USB adapter, part no. 98427, that fits the MS SideWinder Precision Pro, part no. X03-57540, product id. starting w/ 85791.

This is how it connects:

USB pin 1 (red, Vbus) --> GP pin 1 (5V)
USB pin 2 (white, D-) --> GP pin 14 (BY2)
USB pin 3 (green, D+) --> GP pin 10 (BY1)
USB pin 4 (black, GND) --> GP pin 4 (GND)

USB shield --> GP shield
GP pin 2 (BX1) --> GP pin 7 (BX2)

GP = Gameport. Colors in () are the USB cable colors.

To make an adapter you need a female DB15 connector (preferably w/ a plastic shell) and a USB cable w/ an A connector on one end (the flat connector that plugs into your computer.) Cut the cable so you end w/ a 4-5\" pigtail on the USB A connector. Wire it to the DB15 connector according to the list above.

Edit: corrected according to the posts below.
Looking at the adapter again I recognized something odd -- pins 2 (BX1) & 7 (BX2) are populated as well so I did a quick cross check: the adapter shorts these together, probably as the signal for the stick to go USB :) Add a short on the GP connector betw. 2 & 7 as well.

There are two \"flavors\" of MS Sidewinder Precision Pro joysticks. They look and feel the same, but you can look on the bottom panel to see which one it is:

1. Product ID starts with \"85791\" - this is one being referred to above. It's natively USB-compatible, and originally came with a Gameport->USB adapter.

2. Product ID starts with \"97462\" - this one is not natively USB-compatible, and will need one of Grendel's adapters to work via USB.
*/

######################## problem with aptitude #################################################
I just ran apt-get upgrade on my debian 5 vps and i got this error: tar:
  unrecognized option `--warning=no-timestamp'
I've just had exactly this problem. As Gilles suggested, upgrading tar is the answer but
(surprise surprise) tar can't be upgraded in the usual way because dpkg requires
version 1.23 or later before it'll unpack and install the latest tar deb. dpkg really needs an
explicit dependency to ensure that when a later version of dpkg is installed, the latest
tar version goes on first, but I guess it's a risk with combining different testing and unstable
releases in unsupported ways. It's still undesirable and this seems an unfortunate way to cripple
 a system.

My first idea was to look for the latest binary of tar at the GNU project, but unfortunately
they only have source downloads which aren't helpful if (like me) you don't have the various
compiling tools installed.

If you download the latest tar binary deb to match your system from packages.debian.org and drop it
into a temporary place somewhere (just to be tidy), you should be able to get inside it with
the ar command. eg. 'ar x tar_1.26-2_amd64.deb' in my case. Then unpack the resulting data.tar.gz
file with a command like 'tar zxvf data.tar.gz', using your existing earlier version of tar which
should work as long as you don't try and do anything silly like use a --warning parameter
that won't be available until version 1.23. :) This will then let you get at the tar binary,
which (within the data.tar.gz file) was probably at bin/tar. Having done this, I added the path
for that binary to the front the PATH variable in my shell, which in my bash shell could be done
with the command 'export PATH=/root/temp/bin:$PATH', but adjust the path to fit wherever the new
tar binary is now sitting. After that running a regular 'dpkg --install tar_1.26-2_amd64.deb'
worked wonders, because dpkg will look in the path and find the latest tar binary before it
finds the older version in the regular bin path.
I hope this helps.

######### tips for iriss ######
for passing the content of a file use cat
/exec -msg nick/channel cat /route/to/file
or
/exec -o cat /route/to/file [this output in the same window]

extend this for more interesting things of exec
for the fucking facebook
-1149677633@chat.facebook.com

### "Jar files" ###

Creating View and Extract a JAR File
	The basic format of the command for creating a JAR file is:
        	jar cf jar-file input-file(s)
	View content
		jar tf jar-file
	Extract files
		jar xf jar-file [archived-file(s)]



#### Server ####
runing awstats for update
/usr/lib/cgi-bin/awstats.pl -update -config=ambagasdowa.dyndns.org



#### How to run multiple version of firefox (beta version for example) #####

Use common sense , yes that

just remenber to use -no-remote and a another perfil

-CreatePerfil firefox_beta

appear to be a problem with firefox 2

$ ./firefox -no-remote -P firefox-2.0 ./firefox-bin: error while loading shared libraries:
libstdc++.so.5: cannot open shared object file: No such file or directory
Whooops. Grandpa Firefox 2.0 says it won’t run, unless it has the libstdc++.so.5  library.
However, Ubuntu 9.10 comes with a newer version of libstdc++, and the obsolete release is no
longer available. But that’s no big deal, as I can just get it elsewhere — for example, at
packages.debian.org:

http://ftp.us.debian.org/debian/pool/main/g/gcc-3.3/libstdc++5_3.3.6-18_i386.deb

So let’s grab it:

$ cd .. $ wget
http://ftp.us.debian.org/debian/pool/main/g/gcc-3.3/libstdc++5_3.3.6-18_i386.deb

Since it’s a .deb package, I could simply install it with dpkg — but, I want to keep my
system sterile clean and not clutter it with outdated packages that only some old Firefox
release needs. Let’s have a look at the files that this package would install — I’ll use a
combination of ar and tar (in case you’d be interested, this blog post at G-Loaded Journal
explains the use of these commands):

$ ar p libstdc++5_3.3.6-18_i386.deb data.tar.gz | tar zt ./ ./usr/ ./usr/share/
./usr/share/doc/ ./usr/share/doc/libstdc++5/ ./usr/share/doc/libstdc++5/README.Debian
./usr/share/doc/libstdc++5/TODO.Debian ./usr/share/doc/libstdc++5/copyright
./usr/share/doc/libstdc++5/changelog.Debian.gz ./usr/lib/ ./usr/lib/libstdc++.so.5.0.7
./usr/lib/libstdc++.so.5

Apparently, there’s a couple documentation files that I can ignore. What I’m interested in
are the two library files in ./usr/lib — libstdc++.so.5.0.7 and libstdc++.so.5 (actually,
that’s just one library file, as libstdc++.so.5 is a symbolic link to libstdc++.so.5.0.7),
and I’m going to extract just those two files. Moreover, I don’t want tar to create the
usr/lib directory structure (as it normally would), I just want the two files to be placed in
the firefox-2.0 directory, and for all that to happen I’ll use this lengthy command:

$ ar p libstdc++5_3.3.6-18_i386.deb data.tar.gz | tar zx ./usr/lib/libstdc++.so.5.0.7
./usr/lib/libstdc++.so.5 --transform 's!./usr/lib/!firefox-2.0/!'

What this command does is it extracts the two library files from the package, then translates
their full paths, substituting ./usr/lib with firefox-2.0, so the files should end up where I
want them. Let’s see:

$ ls firefox-2.0/libstdc++* firefox-2.0/libstdc++.so.5  firefox-2.0/libstdc++.so.5.0.7

Great. Now I can try launching Firefox 2 again, but I also have to tell it that it should
look for the libstdc++.so.5 library file in its own directory. There’s an environmental
variable that serves this purpose called LD_LIBRARY_PATH — all I need to do is set it to “.”
(which corresponds to the current directory) and run Firefox with that setting:

$ cd firefox-2.0 $ LD_LIBRARY_PATH=. ./firefox -no-remote -P firefox-2.0

Success! We can now party like it’s 2008.


############### For a list of charset ##############################
iconv -l

for an alarm
sleep [time in seconds] && mpg123 -C /route/to/file.ogg



############## For study
"monitor memory usage"
$ watch vmstat -sSM


iotop para ver el acceso al disco
htop o top para ver el consumo de memoria
nethogs para el consumo de red

scp

sleep 10 ;echo "Hola";

"adding a user to /etc/group"

adduser user group
example
adduser ambagasdowa lpadmin

"Printers"

--Para hp c4280
- Con la máquina encendida, pulse simultáneamente las teclas de ENCENDER y CANCELAR hasta que aparezca en pantalla Enter special key combo. Entonces suelte las teclas y pulse consecutivamente las teclas del lateral de la izquierda de la pantalla de la impresora en el siguiente orden: GRIS - AZUL -VERDE y se introducirá en un menú interno de la impresora.

- Para desplazarnos por dicho menú, lo haremos con la tecla AZUL hasta que vea SPECIAL REPORTS y pulse la tecla VERDE a modo de OK.

- Nuevamente desplácese con la tecla AZUL hasta encontrar la opción SELFTEST y pulse la tecla VERDE.

- La impresora imprimirá el informe solicitado.
#################################################

para samsung ml2165

Mantener Presionado el boton Imprimir pantalla
firmware info
blink- no blink - blink - Release
Toner info
blink - no blink - blink - no blink -release


"Printing examples"
 lp -o page-ranges=1,2 -o landscape -o scaling=75 filename.ext

"lpoptions usage example"

lpoptions -p printer_name -o ColorMode=[Color/Monochrome] -o Quality=[normal/draft]

"print a text"
Recomended write text withd=60 columns
for  properties of a document use
file -ib filename
for converting encoding use
iconv -f utf8 -t latin1 -o /path/to/output_file /path/to/source/file
then we can use enscript for converting to a ps file
enscript --margins=left:rigth:top:bottom -[2/3/4] --[landscape/portrait] --encoding=latin1 \
         --font=Courierbold10/11  --media=letter -B -p /path/to/output.ps
  see man enscript for details

"Recording from console"
use arecord
arecord -f cd -t wav --device=default /path/to/output.wav

device can be --device=default:card-name example --device=default:default
 for my usb extra audio card

"updating the initramfs like mkinitramfs on suse"

update your initramfs image
# update-initramfs -u
on Debian/Ubuntu/Xandros) to update your initrd image

"Updating udev"
AT YOUR OWN RISK, you can force the installation of this version of udev
WHICH DOES NOT WORK WITH YOUR RUNNING KERNEL AND WILL BREAK YOUR SYSTEM
AT THE NEXT REBOOT by creating the /etc/udev/kernel-upgrade file.



"Removing services "
For Remove a service  do
update-rc.d -f "service_name" remove
for restoring a service
update-rc.d -f "service_name" defaults
for searching a service ex apache
ls -l /etc/rc?.d/\*apache2

Note: This command will only disable the service until next time the service is upgraded. If you want to make sure the service won't be re-enabled upon upgrade, you should also type the following:
# update-rc.d apache2 stop 80 0 1 2 3 4 5 6 .

2.2. Custom priorities

But as you can see, the default value is 20 which is pretty different than 91 ... a S20 link is started before a S91 and and K91 is kill before K20.
To force apache2 to be started with priorities 91 for both Start and Kill, we need to use the following command:

# update-rc.d apache2 defaults 91
Adding system startup for /etc/init.d/apache2 ...
/etc/rc0.d/K91apache2 -> ../init.d/apache2
/etc/rc1.d/K91apache2 -> ../init.d/apache2
/etc/rc6.d/K91apache2 -> ../init.d/apache2
/etc/rc2.d/S91apache2 -> ../init.d/apache2
/etc/rc3.d/S91apache2 -> ../init.d/apache2
/etc/rc4.d/S91apache2 -> ../init.d/apache2
/etc/rc5.d/S91apache2 -> ../init.d/apache2
2.3. Different priorities for Start and Kill

Alternatively, if you want to set different priorities for Start than for Kill, let say Start with 20 and Kill with 80, you will need to run:

# update-rc.d apache2 defaults 20 80
Adding system startup for /etc/init.d/apache2 ...
/etc/rc0.d/K80apache2 -> ../init.d/apache2
/etc/rc1.d/K80apache2 -> ../init.d/apache2
/etc/rc6.d/K80apache2 -> ../init.d/apache2
/etc/rc2.d/S20apache2 -> ../init.d/apache2
/etc/rc3.d/S20apache2 -> ../init.d/apache2
/etc/rc4.d/S20apache2 -> ../init.d/apache2
/etc/rc5.d/S20apache2 -> ../init.d/apache2
3. Specifying custom runlevels

Finally, if you only want to Start and Kill on specific runlevels, like for instance starting apache with priority 20 on runlevels 2, 3, 4 and 5 and Kill with priority 80 on runlevels 0, 1 and 6:

# update-rc.d apache2 start 20 2 3 4 5 . stop 80 0 1 6 .
Adding system startup for /etc/init.d/apache2 ...
/etc/rc0.d/K80apache2 -> ../init.d/apache2
/etc/rc1.d/K80apache2 -> ../init.d/apache2
/etc/rc6.d/K80apache2 -> ../init.d/apache2
/etc/rc2.d/S20apache2 -> ../init.d/apache2
/etc/rc3.d/S20apache2 -> ../init.d/apache2
/etc/rc4.d/S20apache2 -> ../init.d/apache2
/etc/rc5.d/S20apache2 -> ../init.d/apache2

Or, to start with priority 20 for runlevel 2, 3 and 4 and priority 30 for runlevel 5 and kill with priority 80 for runlevel 0, 1 and 6:

# update-rc.d apache2 start 20 2 3 4 . start 30 5 . stop 80 0 1 6 .
Adding system startup for /etc/init.d/apache2 ...
/etc/rc0.d/K80apache2 -> ../init.d/apache2
/etc/rc1.d/K80apache2 -> ../init.d/apache2
/etc/rc6.d/K80apache2 -> ../init.d/apache2
/etc/rc2.d/S20apache2 -> ../init.d/apache2
/etc/rc3.d/S20apache2 -> ../init.d/apache2
/etc/rc4.d/S20apache2 -> ../init.d/apache2
/etc/rc5.d/S30apache2 -> ../init.d/apache2
/////////////////////////////////////////////////////////////////////////
to hibernate

do

#pm-hibernate

for changing worldlist use
select worldlist <blank> >>> research about this
to reconfigure exim use >>> 'dpkg-reconfigure exim4-config'
research about 'set-user-ID bit set'
research realtime process with jackd
To enable spanish manpages set the enviorement variable LC_MESSAGESS to
es or es_ZZ where ZZ is your country code regards
for using a proxy the format is http://login:pass@proxy
NIS or NFS using portmap edit /etc/default/portmap
using smbpasswd to configure pass

###########################################################################################
"This is freaking awesome !!!	"

ADS == Alternate Data Stream

"Usage"
type > \nt\hideme>watchme_file.ext:giveme_a_name.my_ext
example
type > file.avi/exe > true_file.txt:file.avi/exe
to exec

start true_file.txt:file.avi/exe

try this on a *nix-box is the same but with more options
mplayer file:hide-file
for unpack
more < true_file.txt:file.avi/exe > file.avi/exe
###########################################################################################
Calling with skype in Mexico

cells add "1" between country-code and LADA-Area-code
[country-code][1][LADA][phone-number]
example
my own
[52][1][228][xxxxxxx]
landing phone
[country-code][LADA][phone-number]
[52][228][xxxxxxx]

###########################################################################################

"Configure Static Routes In Debian or Red Hat Enterprise Linux"
by LINUXTITLI on APRIL 13, 2006 · 29 COMMENTS

Static routes improves overall performance of your network (especially bandwidth saving). They are also useful in stub networks (i.e. there is only one link to the network). For example, each LAN (located at different offices) is connected to HQ IDC (Internet data center) using single T1/LL/Wan links.


For example under Red Hat/Fedora Linux you can add static router for eth0 network interface by editing /etc/sysconfig/network-scripts/route-eth0 file. Under Debian Linux add static route by editing /etc/network/interface file.



Task: Display Current Routing Table Using ip command

By using the ip command, you can setup and view static route. For example, to display current routing table you can type command:
# ip route show

Sample output:

192.168.2.0/24 dev eth1 proto kernel  scope link  src 192.168.2.1
192.168.1.0/24 dev eth0  proto kernel  scope link  src 192.168.1.2
default via 192.168.1.254 dev eth0
You can add static route using following command:
ip route add {NETWORK} via {IP} dev {DEVICE}
For example network 192.168.55.0/24 available via 192.168.1.254:
# ip route add 192.168.55.0/24 via 192.168.1.254 dev eth1

Alternatively, you can use old good route command:
# route add -net 192.168.55.0 netmask 255.255.255.0 gw 192.168.1.254 dev eth1

Linux Persistence Routes

The drawback of 'ip' or 'route' command is that, when Linux reboots it will forget static routes. So store them in configuration file. Static routing describes a system that does not implement adaptive routing. In these systems routes through a data network are described by fixed paths (statically). These routes are usually entered into the router by the system administrator


Red Hat (RHEL) / CentOS / Fedora Linux Persistence Static Routing

You need to open /etc/sysconfig/network-scripts/route-eth0 file to define static routes for eth0 interface:
# cat /etc/sysconfig/network-scripts/route-eth0

Sample Output:

GATEWAY0=192.168.1.254
NETMASK0=255.255.255.0
ADDRESS0=192.168.55.0

GATEWAY1=10.164.234.112
NETMASK1= 255.255.255.240
ADDRESS1=10.164.234.132
How do I define static routing for network 10.0.0.0/8 via 10.9.38.65 router?
Open /etc/sysconfig/network-scripts/route-eth0:
# vi /etc/sysconfig/network-scripts/route-eth0

Append following line:
10.0.0.0/8 via 10.9.38.65

Save and close the file. Restart networking:
# service network restart

Verify new routing table:
# route -n


Debian / Ubuntu Linux Persistence Static Routing

Open configuration file /etc/network/interfaces
# cat /etc/network/interfaces

Output:

auto eth0
iface eth0 inet static
address 192.168.1.2
netmask 255.255.255.0
gateway 192.168.1.254
up route add -net 192.168.2.0 netmask 255.255.255.0 gw 192.168.2.1
down route del -net 192.168.2.0 netmask 255.255.255.0 gw 192.168.2.1
DEBIAN / UBUNTU LINUX STATIC ROUTING FOR TWO INTERFACES:
auto lo
iface lo inet loopback

auto eth0
iface eth0 inet static
        address 10.9.38.76
        netmask 255.255.255.240
        network 10.9.38.64
        broadcast 10.9.38.79
	### static routing ###
        post-up route add -net 10.0.0.0 netmask 255.0.0.0 gw 10.9.38.65
        pre-down route del -net 10.0.0.0 netmask 255.0.0.0 gw 10.9.38.65

auto eth1
iface eth1 inet static
        address 204.186.149.140
        netmask 255.255.255.240
        network 204.186.149.128
        broadcast 204.186.149.143
        gateway 204.186.149.129
        # dns-* options are implemented by the resolvconf package, if installed
        dns-nameservers 10.0.80.11 10.0.80.12
        dns-search nixcraft.in

###########################################################################################
Register protocol

Linux

All Firefox versions

(Requires certain Gnome libraries to be installed)

    * In a terminal, type:

gconftool-2 -s /desktop/gnome/url-handlers/foo/command '/path/to/app %s' --type String
gconftool-2 -s /desktop/gnome/url-handlers/foo/enabled --type Boolean true

Replace foo on both lines with the protocol you want to register and /path/to/app with the path to the application you want to run.
[edit]
Firefox 3.5 and above

(Works without installed Gnome libraries)

    * Type about:config into the Location Bar (address bar) and press Enter.
    * Right-click -> New -> Boolean -> Name: network.protocol-handler.expose.foo -> Value -> false (Replace foo with the protocol you're specifying)
    * Next time you click a link of protocol-type foo you will be asked which application to open it with.

Firefox versions up to 3.0

(Works without installed Gnome libraries)

    * Type about:config into the Location Bar (address bar) and press Enter.
    * Right-click -> New -> Boolean -> Name: network.protocol-handler.external.foo -> Value -> true (Replace foo with the protocol you're specifying)
    * Right-click -> New -> String -> Name: network.protocol-handler.app.foo -> Value -> /path/to/app (Replace foo with the protocol you're specifying and /path/to/app with the path to the application you want to run)
    * Ensure network.protocol-handler.expose-all is set to true.

###########################################################################################
"HACKING Casio fx-82MS"

La Calculadora CASIO fx-82 MS es la que tienen la mayoría de los estudiantes
de secundaria, terciaria y universitarios…y sabían que CASIO fabrica las calculadoras
científicas “standars” con la misma plaqueta y lo único que hacen es bloquearle algunas funciones a
ciertos modelos y así vender unas a 50$ y otras con las funciones “desbloqueadas” a 120$ o más…Bueno aquí les
traigo el truco que les va a poder “desbloquear” ESAS funciones que su calculadora fx-82MS
tiene escondidas, como
son algunas:

RESOLVER MATRICES

RESOLVER CÚBICAS, CUADRÁTICAS, ETC.

TRABAJAR CON NÚMEROS COMPLEJOS FACILMENTE

y muchas funciones más!!!


LOS PASOS A SEGUIR SON:

1) Poner la calculadora en modo SD

La prendemos —- MODE —- 2


2) Presionar el 0 (cero) y luego apretar el boton M+ (arriba de AC), en la pantalla nos va a ir apareciendo:

n =
1

Seguir apretando “M+” varias veces (va a ir diciendo n= 2, n= 3, …..) hasta que nos diga n=80, luego de ahí si apretamos una vez más nos aparece:

Data FULL

Luego apretamos 1 vez más y nos aparece:

EditOFF ESC
1 2

Aquí presionamos 2…

Ahora en la pantalla tenemos otra vez común, recien estamos en la mitad del TRUCO…

Tenemos que presionar del botón central (4 direcciones) que está abajo de la pantalla la parte superior (flecha para arriba), aquí nos dice:

Freq 80=
1

Ahora empezamos a digitar el número “13″ consecutivamente:

13131313131313….1313131313131

(tener cuidado en no equivocarse en ninguna cifra)

Cuando llegamos al límite de cifras que puede soportar la calculadora (para verificar si escribimos bien, una forma es (no excluyente) que el último número es el 1)

Presionamos “=” (igual) y nos aparece:

Data FULL

Presionamos nuevamente el botón “=” (igual) y nos aparece:

EditOFF ESC
1 2

Aquí presionamos el "0" y luego el "1"

Cuando apretamos el CERO no nos cambia nada en la pantalla, pero cuando presionamos el UNO nos aparece:

1 (y separado por un espacio,el cursor para seguir digitando)

Presionamos “=” (igual) y nos aparece:

Syntax ERROR

Presionamos AC y “supuestamente” queda como siempre que la prendemos está PEROOOOo…

Si apretamos el botón “MODE” aquí están las nuevas funciones!

COMPLX, BASE, MAT, VCT, EQN !!!

Y todas fáciles de aprender a usar por como te va llevando la propia calcu a cargar las variables….

Como todo “Truco” tiene un lado malo, que en este caso es un lado “no tan bueno…” porque cada vez que apretamos el botón “on” se nos van estas “NUEVAS FUNCIONES”

Bueno espero que lo hayan echo y sobre todo que les haya servido…Muchas gracias y cualquier duda estoy para lo que sea.
este truco es bueno para los universitarios o estudiantes de colegio
espero q les sirva y chaooo



###########################################################################################

Changing DNS on Debian

Example: Changing DNS server settings on a Debian server

   1. Edit /etc/resolv.conf:
     sudo vi /etc/resolv.conf
   2. If any nameserver lines appear, write down the IP addresses for future
      reference.
   3. Replace the nameserver lines with, or add, the following lines:
      nameserver 8.8.8.8
      nameserver 8.8.4.4
   4. Save and exit.
   5. Restart any Internet clients you are using.
   6. Test that your setup is working correctly; see Testing your new settings below.
      Additionally, if you are using DHCP client software that overwrites the settings in
     /etc/resolv.conf, you will need to set up the client accordingly by editing the
     client's configuration file. Example: Configuring DHCP client sofware on a Debian server
   1. Back up /etc/resolv.conf:
    sudo cp /etc/resolv.conf
    /etc/resolv.conf.auto
  2. Edit /etc/dhcp3/dhclient.conf:
  sudo vi
  /etc/dhcp3/dhclient.conf
  3. If there is a line
 containing
 domain-name-servers, write
 down the IP addresses for
 future reference.
 4. Replace that line with, or add, the following line
 prepend domain-name-servers 8.8.8.8, 8.8.4.4;
 5. Save and exit.
 6. Restart any Internet clients you are using.

###########################################################################################
"For slow connections on Debian and derivates change the MTU option"
ifconfig eth0 mtu 1500 /*also can be 1492 or 1472 but if you have 576 is only for
modem 56 kbps*/
just testing appears that problems is for dhcp in the option request-mtu from
dhcp server and put again to 572 so comment that part and add in
/etc/networks/interfaces the line
pre-up /sbin/ifconfig eth0 mtu 1500
after inet eth0 dhcp
testing so i'll be back

nmap theory

nmap -O -PN -sS -v -sV -A <ip address>
-O kind of OS -PN treat at alive host -sS -win something- -A os detection -sV service
version -v
verbose

#################################################################################################
/**
 *	Cambiar MAC Address en Linux  (de Forma Permanente)
 */

Editaremos el script de inicio:

    $ sudo gedit /etc/init.d/bootmisc.sh

y al final del archivo, despues de la linea de exit, escribiremos:

    ifconfig eth0 down
    ifconfig eth0 hw ether xx:xx:xx:xx:xx:xx
    ifconfig eth0 up

Donde: xx:xx:xx:xx:xx:xx es la MAC que quieres utilizar permanentemente...

1. Alter Using macchanger

   1. --help Nos da una ayuda acerca de la sintaxis y uso del programa
   2. --version Nos imprime en la pantalla la versión del MACchanger que estamos usando
   3. --endding Esta opción genera una nueva dirección MAC sin cambiar los bit del vendedor
   4. --random Genera una MAC aleatoriamente
   5. --mac XX:XX:XX:XX:XX:XX Aquí nosotros mismos definimos la dirección MAC que queremos para nuestra tarjeta de red

example

Current MAC: 00:00:a9:b5:36:6e (Network Systems Corp.)
Faked MAC:   00:90:6d:d4:18:e8 (Cisco Systems, Inc.)

the trick for permanent

add to /etc/network/interfaces
auto lo ethx
iface ethx inet dhcp
pre-up /sbin/ifconfig ethx hw ether XX:XX:XX:XX:XX:XX

for gateway configuraton command
first config via ifconfig tool
ifconfig ethx 192.168.0.0/24 netmask 255.255.255.0
the add the gateway
route add default gw "gw-ip" interface
done

Have a lot of fun !!

#################################################################################################
"Modification of permissions"

chmod --

chown user:group file

example
chown user_name:user_name my_file.cpp
chown user_name:root my_file.cpp

"Modify the fsck at start"
// stop fscheck on startup
tune2fs -c mounts_count /dev/my_dev




[www]
path = /var/www/
public = yes
writable = yes
comment = smb share
printable = no
guest ok = yes
create mode = 0644
directory mode = 0755
force group = www-data
force user = www-data

restart:

# /etc/init.d/samba restart

and to be on the safe side

# chown -R www-data /var/www/
# chgrp -R www-data /var/www/

No you should be able to read, write and create to /var/www/ with owner and groups assigned to www-data with Windows/Samba


###########################################################################################
To find PKG_CONFIG_PATH

echo $PKG_CONFIG_PATH

To add to PKG_CONFIG_PATH

PKG_CONFIG_PATH=$PKG_CONFIG_PATH:/my/additional/config
######################################################################################
"fixing Nvida Tv-out HUE Blue problem "

Adding these lines to xorg.conf beneath the DEVICE section for my TV solved the issue:

Option "TVOutFormat" "COMPONENT"
Option "TVStandard" "HD480i"

appears to fail the autodetection of type of tv

Option "TVOutFormat" "SVIDEO" or Option "TVOutFormat" "COMPOSITE"


"Start other x session"

startx -- :[num]
example:
startx -- :1 //for another desktop instance


"Auto login and startx without a display manager"


for stopping gdm kdm etc

To disable the login manager on your Debian/Ubuntu system (which in turns prevents X
server to start automatically) give the following command:

# update-rc.d -f gdm remove

Enable/Restore login manager

# update-rc.d gdm defaults

for fb
/sbin/modprobe intelfb
mknod /dev/fb0 c 29 0


edit the getty section of /etc/inittab
comment out
#1:2345:respawn:/sbin/getty 38400 tty1
below it add
1:2345:respawn:/bin/login -f YOUR_USER_NAME tty1 </dev/tty1 >/dev/tty1 2>&1

now edit .bash_profile and at the bottom add
startx

Thats all folks! Feel free to share your method for doing the same thing.

I add the following code in ~/.bashrc, so X starts only for tty1

    if [ -z "$DISPLAY" ] && [ $(tty) == /dev/tty1 ]; then
            startx
//        or any other app for example exec advmenu_fb_version against startx
    fi

and add a .xinitrc for manage autostart exec programs

this is an example of .xinitrc file in your $HOME dir

xsetroot -solid black &
xloadimage -onroot $HOME/logo.png
##"a more smooth viewver come's in the eterm package"
Esetroot -f file.png &
#/usr/bin/konsole &
exec advmenu
/sbin/halt -p

#xrdb -load $HOME/.Xresources
#xbiff -geometry -430+5 &
#oclock -geometry 75x75-0-0 &
#xload -geometry -80-0 &
#xterm -geometry +0+60 -ls &
#xterm -geometry +0-100 &

#exec twm

"For DISPLAY issues"

for star an apps in display :0.1
example:

cmd $ DISPLAY=:0.1 Esetroot -f ~/background.png & ; DISPLAY=:0.1 advmenu

can use DISPLAY=192.168.0.1:0.1 $cmd ... etc

or if the program have the parameter --display

for start a diferent wm or etc
use
 edit .xinitrc with the parameters
example :

exec advmenu

then start

startx -- :1 # for start a new desktop in tty8

And for auto-start apps with xinitrc add

DISPLAY=:0.1 /bin/bash scripts/work/my_app.sh &


"On the command line"

If you have a working ~/.xinitrc, but just want to try other WM/DE you can run it
by issuing xinit followed by the path to the window manager:

"xinit /full/path/to/window-manager"

$ xinit /usr/bin/xterm -- :0
$ xinit /usr/bin/icewm -- :1


Note that the full path is required. Optionally, you can pass options
to the X server after appending -- - e.g.:

xinit /usr/bin/enlightenment -- -br +bs -dpi 96
The following example ~/.xinitrc shows how to start a particular
window manager with an argument:

~/.xinitrc

#!/bin/sh
#
# ~/.xinitrc
#
# Executed by startx (run your window manager from here)

if [[ $1 == "fluxbox" ]]
then
  exec startfluxbox
elif [[ $1 == "spectrwm" ]]
then
  exec spectrwm
else
  echo "Choose a window manager"
fi

Using this example you can start fluxbox or spectrwm
with the command xinit fluxbox or xinit spectrwm.


Have a lot of fun!
// ---------------------------------------------------------------------------------------------------
/** NOTE ### ::: Android Section ::: ### */



I'm using adb to sync music on an android phone. Essentially, I rm the existing music directory and push replacement music files.
I'd like to be able to use adb to force a rescan, so that the google music player (and other apps) works properly with the new songs and playlists.
According to How can I refresh MediaStore on Android? you can force a rescan by broadcasting an appropriate intent.
adb provides 'shell am broadcast', which would seem to allow me to force a rescan from adb.
Alternatively I could run a rescan app or reboot, but I'd like to trigger the rescan from adb
What adb command should I issue? The music files and playlists are all in /sdcard/music.

Refresh Android mediastore using adb @link: https://stackoverflow.com/questions/17928576/refresh-android-mediastore-using-adb
  The rescan apps use a media mount intent to kick off the media scanner. You can use am broadcast to send the same intent.
  The command is:

adb shell am broadcast -a android.intent.action.MEDIA_MOUNTED -d file:///sdcard



// ---------------------------------------------------------------------------------------------------
### ::: Xbox section ::: ###
/**
put hir all xbox - linux tools
*/


for make an xiso image game

use exctract-xiso

example

extract-xiso -c path/to/dir_whit_files /path/to/output.iso

see --help for more information

  Backups of Xbox 360

growisofs -use-the-force-luke=dao -use-the-force-luke=break:1913760 -dvd-compat -speed=2 -Z /dev/dvd=~/FULL/PATH/TO.ISO

.XMV files.. clearly a version of Windows Media Video 2 format…  No direct editor currenly exists… however.. the format is well documented by Zziggy00 of X-S.

 .xmv format..

File Type : XMV
Duration : 73773 ms
Packet Size : 24576 byte
Stream Count : 2

Stream #0
------------------------------
Type : Audio
Format : ADPCM
Channel Count : 2
Samples Per Second: 44100
Bits Per Sample : 4

Stream #1
------------------------------
Type : Video
Format : WMV2
Size : 320 x 240


### ::: PlayStation Section ::: ###

for rip a psx disc

use cdrdao

$ cdrdao read-cd --read-raw --datafile /path/to/file.bin --device /dev/cdrom --driver
generic-mmc-raw /path/to/file.toc

 // bin to iso use bchunk

 firts convert toc to a cue file
 toc2cue file.toc file.cue
 then make the iso

 bchunk -v -p file.bin file.cue file.iso

 check if works for PS2 dvd's

 TODO for playstation2

  for now using CDVDisoEFP in pcsx2 built pluging --investigate this ---
 ok, rip with dvdecrypter for windows works
 and  for linux a simple dd  command works
 example
 dd if=/dev/sr0 of=/tmp/iso/rip_game.iso


###############################################################################
Linux MAME Cab HowTo
[Contents]

    1. About this HowTo
    2. Install Debian
    3. Configuring Debian
    5. Install Advance MAME/Menu
    6. Boot Setup
    7. Tips and Suggestions
    8. HELP!
    9. Changes
                [About this Howto]
  This document should be helpful to you if you
  wish to replicate my MAME setup in some way. I
  have been using my MAME cabinet for about 2
 years, with various software configurations. This
  is the best setup I have used so far. This
  document is not intended for a Linux newcomer.
  You will need to be at least vaguely familiar
  with the command line and your hardware (Debain
  doesn't auto detect your hardware.... yet).
  The key features of this setup are:
  * Linux OS
  * Boots directly into the
    MAME frontend
  * Shuts computer down
    when exiting frontend
  * ROM and MP3
    updates via
   FTP (or ssh)
  [Install  Debain]
   First things first. We need to install an OS. I
  have  chose to use Debian. I use it for work,so I am familiar with it.If you  are
  comfortable with Linux and use another distro you should be able to use most of these instructions with a little bit of tinkering or Google-ing.
  I am not going to walk you through a Debian install, sorry. But the good news is that it is fairly simple. With the exception of partitioning and configuring drivers you should be able to hit ENTER for every option (the defaults are almost always correct). With my setup the only drivers I had to install were for my NIC card, sound card, and APM (power management, so it will shutdown correctly). You could go ahead and add a normal user named "mame" when it asks you. You will not need to run tasksel or dselect.
 [Install Required Software]
 After we install debian we need to install some necessary software. We will need a couple things from the unstable version of Debain (don't let this scare you, unstable in Debian speak usually means "pretty stable, just not really really really stable"). Go ahead and open up your source list
 #vi /etc/apt/sources.list
 Then simply change everything that says stable to unstable. Go ahead and exit vi with a :x and run an update on your package list.
 #apt-get udpate
 Finally we will install the software need.
 #apt-get install xserver-xfree86 proftpd libsdl-dev libasound alsa-utils xloadimage libc6 wget
 Debian will run you though an X setup (among other things), just make sure you chose the correct video driver (mine was ati). If you need to revisit the X setup simply run dpkg-reconfigure xserver-xfree86 or xf86setup or just edit your /etc/X11/XFree86-4 file
     [Configuring Debian]
 We need to change the way a few things act. First, we don't want XDM starting at boot time.
 #rm /etc/rc.2/S99xdm
 Now we need to tell the system to let a normal user start X from init.d. Open the
XWrapper.config file.
 #vi /etc/X11/XWrapper.config
 Change this line
 allow_users=console
 To this
 allow_users=anybody
 Now we need to give non root access to the halt command.
 #dpkg-statoverride --update --add root root 04755 /sbin/halt
 If you didn't create a mame user you should do so now
 #adduser mame
 Give the mame user access to the sound card
#usermod -G audio mame
[Install Advance MAME/Menu]
Download the latest version of Advance MAME and Advance Menu from http://advancemame.sourceforge.net/. Then simply move it over to your MAME box via FTP (you did install proftp right? you should be able to connect as your mame user). Then move it to the /usr/local directory where we will untar it.
#mv /home/mame/advance* /usr/local/
#cd /usr/local
#tar xvzf advancemame-0.77.0-linux-pentium.tar.gz
#tar xvzfadvancemenu-2.2.14-linux-pentium.tar.gz
                     Finaly, lets install it!
 #cd advancemame-0.77.0-linux-pentium
 #./configure
 #make install
 #cd ../advancemenu-2.2.14-linux-pentium
 #./configure
 #make install
You should now create your config files for advmenu and advmame. One way to do this is to run advmenu and advmame, make sure you are logged in as the mame user to do this. Other wise you could create the config directory and use my config files, they are ready-to-use assuming you have used my instructions and created a user named "mame". Also, my version of advmenu.rc has a different color scheme than the original, with a black background, I didn't like all that white. I also made the "1" key (first player start) enter a game. You will also want to create the directories used by mame (roms, snap, etc...).
  #su mame
  #cd /home/mame
  #mkdir .advance
  #cd .advance
  #wget http://www.bernieberg.com/mame/advmenu.rc
  #wget http://www.bernieberg.com/mame/advmame.rc
   #mkdir artwork cfg diff hi image inp lowcase memcard mp3 nvram rom sample snap sta
   [Boot Setup]
I believe this is the most important part... Getting it to boot directly into our front end and getting it to shutdown when we exit out frontend. Once you know how it is done it is very simple. First we will tell X to start as the user "mame". Create a file in /etc/init.d/
 #vi /etc/init.d/mame
  Add the folowing line to this file
                                                                                            su mame startx
 Exit vi by typing :x. Now lets link it from the rc directory.
 #cd /etc/rc2.d
 #ln -s ../init.d/mame S99mame
The next thing we will do is tell X to set a background picture (so the screen isn't' so boring while games load, this is optional), start advmenu, and to shut the computer down when we exit advmenu. Lets create the .xinitrc file that will do this.
#vi /home/mame/.xinitrc
   Add the following lines to this file
  xloadimage -onroot /home/mame/mamelogo.jpg
  advmenu
 /sbin/halt -p
Exit vi by typing :x. Keep in mind that the image will need to be the size of your resolution if you don't want it tiled. I am using this picture courtesy of ZAKK, resized to 1024x768.
 [Tips and Sugestions]
Uhhh, is that it? Well yeah, pretty much. You can now add your roms and mp3s (to play randomly in advmenu) via FTP. If you got your roms from the same place I did you also got some images all zipped up (for the snap, marquee, etc... directories). You will need to unzip them. If you want to do this from the Linux box just to an apt-get install unzip to get the unzip program, usage is self explanatory.
Also, if your images are like mine were when you unziped them you will have some issues. Advmenu (like most Linux apps) is case sensitive, and my images were all mixed case. You can use this lowercase script I found on the internet to make them all lowercase.
[HELP!]
Stuck? Need some help? Try these links:
Google
* A.G.M archives
* Debian Boot CDs
* HowTo install Debian
* Advance MAME
If you are still stuck you can ask me a question directly by using my message board.
[Changes]
Hey, nobody is perfect right?
* 12/08/03 - Initial Version
* 12/17/03 - Fixed some formating errors. Fixed the libsdl-dev typo (it said libsdk-dev, oops). Added the full path to the halt command, only root has /sbin in the path.

########################################################################################################
How to join avi's

### "For conversion with ffmpeg" ###

Getting infos from a video file
ffmpeg -i video.avi
Turn X images to a video sequence
ffmpeg -f image2 -i image%d.jpg video.mpg
This command will transform all the images from the current directory (named image1.jpg, image2.jpg, etc…) to a video file named video.mpg.

Turn a video to X images
ffmpeg -i video.mpg image%d.jpg
This command will generate the files named image1.jpg, image2.jpg, …

The following image formats are also availables : PGM, PPM, PAM, PGMYUV, JPEG, GIF, PNG, TIFF, SGI.

Encode a video sequence for the iPpod/iPhone
ffmpeg -i source_video.avi input -acodec aac -ab 128kb -vcodec mpeg4 -b 1200kb -mbd 2 -flags +4mv+trell -aic 2 -cmp 2 -subcmp 2 -s 320x180 -title X final_video.mp4
Explanations :

Source : source_video.avi
Audio codec : aac
Audio bitrate : 128kb/s
Video codec : mpeg4
Video bitrate : 1200kb/s
Video size : 320px par 180px
Generated video : final_video.mp4
Encode video for the PSP
ffmpeg -i source_video.avi -b 300 -s 320x240 -vcodec xvid -ab 32 -ar 24000 -acodec aac final_video.mp4
Explanations :

Source : source_video.avi
Audio codec : aac
Audio bitrate : 32kb/s
Video codec : xvid
Video bitrate : 1200kb/s
Video size : 320px par 180px
Generated video : final_video.mp4
Extracting sound from a video, and save it as Mp3
ffmpeg -i source_video.avi -vn -ar 44100 -ac 2 -ab 192 -f mp3 sound.mp3
Explanations :

Source video : source_video.avi
Audio bitrate : 192kb/s
output format : mp3
Generated sound : sound.mp3
Convert a wav file to Mp3
ffmpeg -i son_origine.avi -vn -ar 44100 -ac 2 -ab 192 -f mp3 son_final.mp3
Convert .avi video to .mpg
ffmpeg -i video_origine.avi video_finale.mpg
Convert .mpg to .avi
ffmpeg -i video_origine.mpg video_finale.avi
Convert .avi to animated gif(uncompressed)
ffmpeg -i video_origine.avi gif_anime.gif
Mix a video with a sound file
ffmpeg -i son.wav -i video_origine.avi video_finale.mpg
Convert .avi to .flv
ffmpeg -i video_origine.avi -ab 56 -ar 44100 -b 200 -r 15 -s 320x240 -f flv video_finale.flv
Convert .avi to dv
ffmpeg -i video_origine.avi -s pal -r pal -aspect 4:3 -ar 48000 -ac 2 video_finale.dv
Or:

ffmpeg -i video_origine.avi -target pal-dv video_finale.dv
Convert .avi to mpeg for dvd players
ffmpeg -i source_video.avi -target pal-dvd -ps 2000000000 -aspect 16:9 finale_video.mpeg
Explanations :

target pal-dvd : Output format
ps 2000000000 maximum size for the output file, in bits (here, 2 Gb)
aspect 16:9 : Widescreen
Compress .avi to divx
ffmpeg -i video_origine.avi -s 320x240 -vcodec msmpeg4v2 video_finale.avi
Compress Ogg Theora to Mpeg dvd
ffmpeg -i film_sortie_cinelerra.ogm -s 720x576 -vcodec mpeg2video -acodec mp3 film_terminÃ©e.mpg
Compress .avi to SVCD mpeg2
NTSC format:

ffmpeg -i video_origine.avi -target ntsc-svcd video_finale.mpg
PAL format:

ffmpeg -i video_origine.avi -target pal-svcd video_finale.mpg
Compress .avi to VCD mpeg2
NTSC format:

ffmpeg -i video_origine.avi -target ntsc-vcd video_finale.mpg
PAL format:

ffmpeg -i video_origine.avi -target pal-vcd video_finale.mpg
Multi-pass encoding with ffmpeg
ffmpeg -i fichierentree -pass 2 -passlogfile ffmpeg2pass fichiersortie-2


 ### "For cellphones " ###

$ ffmpeg -i ORIGINAL.mp4 -s RESOLUCION -vcodec h263 -acodec libfaac -ac 1 -ar 8000 -r 25 -ab 32k -y SALIDA.3GP

por ejemplo

$ ffmpeg -i video.mp4 -s 176×144 -vcodec h263 -acodec libfaac -ac 1 -ar 8000 -r 25 -ab 32k -y video.3gp

install mencoder mplayer

	cat b1.avi b2.avi b3.avi b4.avi b5.avi b6.avi b7.avi > bloodspell.avi

mencoder -forceidx -oac copy -ovc copy bloodspell.avi.avi -o bloodspell_final.avi

-forceidx force indexing, -oac output audio codec , -ovc output video codec, 'file_input'
 -o outputv file 'path/to/file_output'

/* MP3 section */
// Convert webm to mp3

 for i in $(ls *.webm); do ffmpeg -i "$i" -acodec libmp3lame -ac 2 -ab 128 "${i%webm}mp3";done

// Convert mp4 to mp3

For FFmpeg with Constant Bitrate Encoding (CBR):

 ffmpeg -i video.mp4 -vn -acodec libmp3lame -ac 2 -ab 160k -ar 48000 audio.mp3

or if you want to use Variable Bitrate Encoding (VBR):

 ffmpeg -i video.mp4 -vn -acodec libmp3lame -ac 2 -qscale:a 4 -ar 48000 audio.mp3

The VBR example has a target bitrate of 165 Kbit/s with a bitrate range of 140...185.

//so in a loop

for i in $(ls) ; do  ffmpeg -i "$i" -vn -acodec libmp3lame -ac 2 -ab 160k -ar 48000 "${i%mp4}mp3"; done

// for mkv files

find . -type f -name "*.mkv" -exec bash -c 'FILE="$1"; ffmpeg -i "${FILE}" -vn -c:a libmp3lame -y "${FILE%.mkv}.mp3";' _ '{}' \;


# Creando Screencast en Linux desde la Terminal

ffmpeg -f alsa -ac 2 -i pulse -f x11grab -r 30 -s 1680x1050 -i :0.0 -acodec pcm_s16le -vcodec libx264 -vpre lossless_ultrafast -threads 0 /home/yoyo/Videos/output.mkv

# Flv to mpg

	ffmpeg -i file_to_convert.flv -sameq output_file.mpg


# Rip a DVD

 use dvdbackup
example:
 dvdbackup --input=/dev/sr0 --mirror --verbose --output=/tmp/iso/
// need at least 10 gb of hard disk space

#  Convert any video file to DVD with open source tools
        /*        By Manolis Tzanidakis on April 27, 2006 (8:00:00 AM)
*/
You've just downloaded the new episode of your favorite video podcast, and you'd like to watch it on your big-screen TV. Unfortunately, the video is encoded in XviD or QuickTime format, which your DVD player doesn't support. Don't worry -- here's how you can convert any video file to DVD using dvdauthor and MPlayer.

Packages for both programs are available for most Linux distributions and BSDs, so you can install them on your favorite OS easily. Compiling the programs from source isn't difficult, as long as you get their dependencies right. Both programs provide adequate documentation about the installation. You can burn the final files to a DVD disc with the help of the growisofs utility from the dvd+rw-tools suite.

Converting the files to MPEG-2

First, you must convert your file to MPEG-2 for the video and to AC3 for the audio, in order to be compliant with the DVD video specifications. If the audio on your file is already encoded in AC3 format, you can use it as is without re-encoding it. Run this command to check the audio format of the file:

mplayer -vo dummy -ao dummy -identify your_video.avi 2>&1 | grep AUDIO_FORMAT | cut -d '=' -f 2

If it returns hwac3, the audio part of your file is encoded in AC3, and you can convert the file to MPEG-2 with the following command:

mencoder -oac copy -ovc lavc -of mpeg -mpegopts format=dvd -vf scale=720:576,harddup \
-lavcopts vcodec=mpeg2video:vrc_buf_size=1835:vrc_maxrate=9800:vbitrate=5000:keyint=15:aspect=16/9 \
-ofps 25 -o your_video.mpg your_video.avi

If it isn't encoded in AC3, run this command:

mencoder -oac lavc -ovc lavc -of mpeg -mpegopts format=dvd -vf scale=720:576,harddup \
-srate 48000 -af lavcresample=48000 \
-lavcopts vcodec=mpeg2video:vrc_buf_size=1835:vrc_maxrate=9800:vbitrate=5000:keyint=15:aspect=16/9:\
acodec=ac3:abitrate=192 -ofps 25 -o your_video.mpg your_video.avi

The previous commands create an MPEG-2 file in phase-alternating line (PAL) format with an aspect ratio of 16:9. PAL is used in most of Europe (except France). If you want to create a National Television System Committee (NTSC) DVD, which is the North American video standard, replace scale=720:576 with scale=720:480, keyint=15 with keyint=18, and -ofps 25 with -ofps 30000/1001. If you don't have a wide-screen TV, you should encode your file with an aspect ratio of 4:3 by replacing aspect=16/9 with aspect=4/3.

For more information, check the MPlayer's man page, which provides detailed explanations about each option used in these commands.

This process should take some time to finish. My 1.5GHz Centrino laptop took about 25 minutes to convert a file with a one-hour runtime that was encoded in XviD.

Creating the DVD structure

Now you can use dvdauthor to create the layout of the DVD from the MPEG-2 file of your video. Although you can pass any options to dvdauthor directly from the command line, it's easier and more flexible to create an XML file with the appropriate options instead. Open your favorite editor and create a file called dvd.xml with the following contents:

<dvdauthor>
  <vmgm />
    <titleset>
      <titles>
        <pgc>
          <vob file="your_video.mpg" chapters="0,0:10,0:20,0:30,0:40,0:50" />
        </pgc>
      </titles>
    </titleset>
</dvdauthor>

I split my hour-long video into six 10-minute chapters. Adjust the chapters= option for your video, or remove the option from your XML file if you don't want chapters.

Type dvdauthor -o dvd -x dvd.xml to create the layout of the DVD; the -o switch defines the output directory, and -x is used for the XML file. This command takes five to 10 minutes, depending on your video size and your CPU speed. Once it completes, you'll have a directory named dvd with two subdirectories: AUDIO_TS and VIDEO_TS. Before burning the video to a disc, you can check it by running mplayer dvd:// -dvd-device ./dvd.

If the video plays correctly, you can burn it onto a DVD disc with growisofs by running growisofs -dvd-compat -Z /dev/dvdrw -dvd-video ./dvd/. Make sure to replace /dev/dvdrw with the device name of your DVD recorder.

The only thing left is to make some popcorn, get your favorite beverage, and enjoy the show.


#      Convert to dvd from Linux Questions By ptesone

install

// transcode
// mplayer
// Mjpegtools
// ffmpeg
//  dvd+rw-tools
// Dvdauthor
// some hardrive space


in this example I will be using a 16:9 movie.avi for our conversion. . .

1.) Split the .avi file into 2 separate files, one for video and one for audio:

Code:

transcode -i movie.avi -y ffmpeg --export_prof dvd-ntsc --export_asr 3 -o movie -D0 -s2 -m movie.ac3
-J modfps=clonetype=3 --export_fps 29.97

this will make:
movie.m2v (video)
movie.ac3 (audio)

note: if you're doing a fullscreen (4:3) movie then simply change --export_asr 3 to --export_asr 2

2.) (optional) Extract 5.1 Audio:

Code:

tcextract -d2 -i movie.avi -a0 -x ac3 | tcextract -d2 -x ac3 -t raw > movie.ac3

This is an extra step if you know your .avi file actually has 5.1 surround sound. (Step one only
produces a stereo .ac3 file !)
How can you tell? Do this first:

Code:

mplayer -vo dummy -identify movie.avi 2> /dev/null | grep "5.1 ("

if you get this output then you have 5.1:

Code:

AC3: 5.1 (3f+2r+lfe) 48000 Hz 384.0 kbit/s

if you don't, just ignore this step!

3.) Put the video & audio file back together:

Code:

mplex -f 8 -o dvd_movie.mpg movie.m2v movie.ac3

this will make dvd_movie.mpg ready for DVD authoring. . .

4.) open your favorite text editor and paste the following:

Code:

<dvdauthor dest="DVD">
  <vmgm />
   <titleset>
     <titles>
       <pgc>
         <vob file="dvd_movie.mpg" chapters="0,15:00,30:00,45:00,1:00:00"/>
       </pgc>
      </titles>
   </titleset>
 </dvdauthor>

save the file as: dvdauthor.xml
in the same directory as your movie files
(you can also change the chapters to fit the times of your movie)

5.) Create a DVD directory where your movie files are and do this:

Code:

dvdauthor -x dvdauthor.xml

this will create two AUDIO_TS and VIDEO_TS directories in your DVD directory. . .

6.) Test it & Burn it:

to test it:
Code:

xine dvd:/full/path/to/DVD/VIDEO_TS/
or
mplayer dvd:// -dvd-device ./DVD

"for playing a iso image"
mplayer dvd://1 -dvd-device /path/to/file.iso

the "1" on dvd://1 means play firts title

to burn it:
Code:

growisofs -Z /dev/dvd -dvd-video DVD/
or from iso
growisofs -dvd-compat -Z /dev/dvd=file.iso
growisofs -dvd-compat -Z /dev/dvd=CourtneyDVD.iso -speed=2

note: I like to use DVD-RW discs for a test before I use a real disc. . .
If all goes well, the above will produce movie with no menus, just the movie that should play when
you put in your disc. . .and if there's more than one .avi then simply do this in your dvdauthor.xml
file:

Code:

<dvdauthor dest="DVD">
  <vmgm />
   <titleset>
     <titles>
       <pgc>
         <vob file="dvd_movie_part1.mpg" chapters="0,15:00,30:00,45:00,1:00:00"/>
       </pgc>
       <pgc>
         <vob file="dvd_movie_part2.mpg" chapters="0,15:00,30:00,45:00,1:00:00"/>
       </pgc>
     </titles>
   </titleset>
 </dvdauthor>


If you want only the DVD iso image type


How to make a dvd iso from a /directory/ with VIDEO_TS and AUDIO_TS directories:


mkisofs -dvd-video -udf -o dvd.iso /directory/
or use genisoimage

All filesnames must be in capital letters. If you are using FAT32, you need to add
shortname=mixed to the filesystem
                                                                        kverde

// for tv record

mencoder tv://12 -tv
driver=v4l2:norm=pal-nc:input=0:width=320:height=240:amode=1:audiorate=48000
 -oac lavc -ovc lavc -o prueba.avi

/*Aclaro que lo de tv://12 no se porque es, pero no es el canal 12, me
graba el ultimo que sintonice con algun programa como tvtime.*/

playback with ascii art

$ mplayer -vo aa video.avi

/*fix the width*/
mplayer -vo aa -monitorpixelaspect 0.5 foo.ogv

merge subtitles

**
 Merge video with subtitles

Now it is time to permanently merge the subtitles from the .srt file into your video. The
subtitles will be rendered in white on a gray box. You can influence the color of the box by
changing the value of 200 to some other value between 0 and 255 (150 makes for a dark box,
200 makes for a very transparent box).

mencoder -oac copy -ovc raw -sub my_subs.srt -sub-bg-alpha 150 -utf8 -o  my_video_output.avi
my_video_source.avi

or, if you have .ogv files, write:

mencoder -oac copy -ovc raw -sub my_subs.srt -sub-bg-alpha 150 -utf8 -o  my_video_output.ogv
my_video_source.ogv

In the case that some characteres get not rendered into your video, you must specify exactly
an international font and its location. For example, if you want to use the font
/usr/share/fonts/truetype/arphic/ukai.ttf write:

mencoder -oac copy -ovc raw -sub myvideo.srt -sub-bg-alpha 150 -utf8 -subfont
/usr/share/fonts/truetype/arphic/ukai.ttf -o myvideo4.avi myvideo3.avi

    * Note that myvideo4.avi will be very big and may also have negative colors. Make sure
you have still enough space free on your hard disk.
    * Maybe Ian will make the showmedo player capable of handling selectable subtitles for
different languages so that this step is not longer necessary in the future.
    * If you skip this step, rename your myvideo3.avi into myvideo4.avi

optimize video

    * Now it is time to optimize the video so that it use less disk space. This wil be in
two turns, for better quality. The following lines are just one gigantic long line; use the
\ (Backslash) Character to continue the same command line in a new line of the terminal:

mencoder -oac copy -ovc lavc \
   -lavcopts
vcodec=mpeg4:mbd=2:trell:v4mv:dia=4:predia=4:preme=2:last_pred=3:vqcomp=0.7:cbp:vmax_b_frames=2:vb_strategy=1:precmp=2:cmp=2:subcmp=2:vpass=1:qns=2:vbitrate=900
\
   -ffourcc DIVX myvideo4.avi -o myvideo5.avi

    This step will take very long time.

if you have .ogv files replace .avi with .ogv

    * Now comes the second turn. This parameter -vpass is different this time:

mencoder -oac copy -ovc lavc \
   -lavcopts
vcodec=mpeg4:mbd=2:trell:v4mv:dia=4:predia=4:preme=2:last_pred=3:vqcomp=0.7:cbp:vmax_b_frames=2:vb_strategy=1:precmp=2:cmp=2:subcmp=2:vpass=2:qns=2:vbitrate=900
\
   -ffourcc DIVX myvideo4.avi -o myvideo5.avi

    This step will also take very long

if you have .ogv files replace .avi with .ogv

// an example for on the fly muxing

mencoder -oac copy -ovc lavc -lavcopts vcodec=mpeg4:mbd=2:trell:v4mv:dia=4:predia=4:preme=2:last_pred=3:vqcomp=0.7:cbp:vmax_b_frames=2:vb_strategy=1:precmp=2:cmp=2:subcmp=2:vpass=2:qns=2:vbitrate=900 -sub /path/to/source/video.srt -sub-bg-alpha 255 -utf8 -subfont /usr/share/fonts/truetype/DejaVuSans.ttf -o /path/to/output/video.avi /path/to/source/video.avi


###############################################

du -sm * | sort -n | tail -20

Para mostrar los 20 directorios de mayor tamaño

###############################################

------------------------------------------------------------------------------------------
//for compiling advancemenu in a debian like sysytem

follow the building instruction on the doc of advmenu chapter for building

then compile with the next options
./configure --disable-fb --disable-alsa
make && make install

advmenu (First time to create a advmenu.rc profile)
advmenu (Second time to start the emulator)
review your advmenu.rc for this lines
*device_alsa_device default
*device_alsa_mixer channel
if you have it remove it from advmenu.rc
enjoy!

check for sdl-config too

I know this is a little late but I was getting ready to install Advancemenu here on
Edgy and was searching the forums to see if anyone had any issue before I started.

I found the following information concerning the error you were getting and wanted to
share.

I take no credit for this and will post back if I have the same error and end up
having to follow these steps:

"Running and compiling AdvanceMAME, as well as AdvanceMENU without framebuffer
support.
Code:
./configure --disable-fb

Make sure that you have set the correct environment variables, to execute the command
sdl-config. Or else you will get a error message like that:
Code:
configure: error: no video library found. If you have the SDL library installed
somewhere try using the --with-sdl-prefix option.

Fix it in following order:
Code:
locate sdl-config

Then check if in /etc/profile the correct environment variables are set and if the
directories listed in /etc/profile contain the command sdl-config. Probably not.
Do...
Code:
export PATH="$PATH:/usr/local/bin"

e.g. if /usr/local/bin is the directory where sdl-config is located (it's strange,
because it should be in there by default, and not only /usr/bin or something similar)

Then go again through the configure and compile procedure
Code:

./configure --disable-fb
make
sudo make install

advmenu (First time to create a advmenu.rc profile)
advmenu (Second time to start the emulator)




"Downgrade grub2 to grub legacy

We want to rename the existing /boot/grub directory for use as a backup so go to the Terminal and run the command:

Code:

sudo mv /boot/grub /boot/grub_backup

Now we'll create a new grub directory:

Code:

sudo mkdir /boot/grub

To be certain those two commands were effective you can run the command:

Code:

ls /boot

Note: that's a lower case L.

You can see highlighted in the sample output below that we now have both grub and grub_backup in the boot directory:

Quote:
lance@lance-desktop:~$ ls /boot
/boot:
abi-2.6.31-11-generic memtest86+.bin
config-2.6.31-11-generic System.map-2.6.31-11-generic
grub vmcoreinfo-2.6.31-11-generic
grub_backup vmlinuz-2.6.31-11-generic
initrd.img-2.6.31-11-generic
Likewise if you run:

Code:

ls /boot/grub

You should see that the grub directory is currently empty because we haven't reinstalled grub yet.

Alternatively you can go to Places > Computer > Filesystem > Boot, you should see the folders named "grub" and "grub_backup".

Step #2: Remove Grub 2 packages, purge configuration files, and install legacy Grub packages.

It is a good idea to purge any old Startup Manager configuration files so:

Code:

sudo apt-get --purge remove startupmanager

If it says "not installed so not removed" that's OK, we just wanted to be sure no old configuration files messed with us later on.

Just FYI 'grub-pc' is grub2 and 'grub' is legacy grub.

First remove grub2:

Code:

sudo apt-get --purge remove grub-pc grub-common


You'll be confronted with this text:

Quote:
Do you want to have all GRUB 2 files removed from /boot/grub?
Your system would be then unbootable if you don't install another bootloader. Remove GRUB 2 from /boot/grub?
Yes, you do! We've created a backup anyway.

Now install legacy grub:

Code:

sudo apt-get install grub

You'll notice that installing grub also reinstalls grub-common, that's normal behavior, we just wanted to "purge" the configuration files.

Now:

Code:

sudo update-grub

You'll be asked if you want to create a /boot/grub/menu.lst - yes, you do, so type y and enter!

Step #3: Physically install and setup Legacy Grub.

Now you must actually "install" legacy grub if you're going to use your new legacy grub to boot with and you must know where. In my case I have only one hard drive and I can see how it's recognized by Ubuntu by running the command:

Code:

sudo fdisk -l

(BTW that's a lower case L.) The pertinent part is in bold in this example:

Quote:
lance@lance-desktop:~$ sudo fdisk -l
[sudo] password for lance:

Disk /dev/sda: 80.0 GB, 80026361856 bytes
255 heads, 63 sectors/track, 9729 cylinders
Units = cylinders of 16065 * 512 = 8225280 bytes
Disk identifier: 0x63056305

Device Boot Start End Blocks Id System
/dev/sda1 * 1 2104 16900348+ 7 HPFS/NTFS
/dev/sda2 2883 9729 54998527+ 5 Extended
/dev/sda3 2105 2882 6249285 83 Linux
/dev/sda5 4214 5012 6417967+ 83 Linux
/dev/sda6 5013 7434 19454683+ 83 Linux
/dev/sda7 7435 8199 6144831 83 Linux
/dev/sda8 8200 9576 11060721 83 Linux
/dev/sda9 9577 9729 1228941 82 Linux swap / Solaris
/dev/sda10 2883 4213 10691194+ 83 Linux

Partition table entries are not in disk order
Since /dev/sda is where I'd want grub to be installed, I'd run this command:

﻿
Code:

sudo grub-install /dev/sda

Note: If you're at all unsure about where to install grub then ask for help here on the forums! Better to be sure of what you're doing than to play the trial and error game!

This would be a good time to create a word document of Terminal progress as recommended above if needed for troubleshooting purposes.

Now we need to open a Grub shell as root so run the command:

Code:

sudo grub

Note: I've noticed while working in a grub shell that the enter key in the numerical block of the keyboard doesn't always work properly, so use the enter key just above the right shift key!

Then:

﻿
Code:

find /boot/grub/stage1

That will provide an output like (hd0,2) which you'll need to use in the next step:

﻿
Code:

root (hd0,2)

You see I've used (hd0,2) from the "find" command, of course you'll use whatever the output was in your specific case. Then:

Code:

setup (hd0)

There I've used the hd0 (that is a zero) from the (hd0,2) output from the "find" command.

If correct you should see an output like:

﻿
Quote:
Checking if "/boot/grub/stage1" exists... yes
Checking if "/boot/grub/stage2" exists... yes
Checking if "/boot/grub/e2fs_stage1_5" exists... yes
Running "embed /boot/grub/e2fs_stage1_5 (hd0)"...* 15 sectors are embedded.
succeeded
Running "install /boot/grub/stage1 d (hd0) (hd0)1+15 p (hd0,1)/boot/grub/stage
2 /boot/grub/menu.lst"... succeeded
Done.
Again, this would be a good time to create a word document of Terminal progress as recommended above just for future troubleshooting purposes because:

If that failed you will likely not be able to boot your machine until we get it straightened out!

Next just type:

Code:

quit

To exit the Grub shell.
################################################
Installing backports on debian


Using backports.org is very simple:

If you want to get announcements about security updates on backports.org subscribe to the
backports-security-announce mailinglist.

Please report bugs that you found in the packages to the backports-users mailinglist and NOT
to the Debian BTS!

1. Add this line

deb http://www.backports.org/debian lenny-backports main contrib non-free

to your /etc/apt/sources.list.

2. Run apt-get update

3. All backports are deactivated by default (i.e. the packages are pinned to 1 by using
NotAutomatic: yes in the Release files, just as in experimental). If you want to install
something from backports run:

apt-get -t lenny-backports install “package”

Of course, you can use aptitude as well:

aptitude -t lenny-backports install “package”

Install the Debian backports keyring
As root, type:

apt-get install debian-backports-keyring

This will install the debian-backports-keyring package, adding the key to your trusted
repository keys.

 If you want apt to verify the downloaded backports you can import backports.org archive’s
key into apt:

apt-get install debian-backports-keyring

or

gpg --keyserver hkp://subkeys.pgp.net --recv-keys 16BA136C
gpg --export 16BA136C | apt-key add -

or

wget -O - http://backports.org/debian/archive.key | apt-key add

###############################################
installing reposytories for debian lenny



#################################################
You can turn the bell off in /etc/inputrc, no need to blacklist modules. The setting is
already there, but commented-out by default:

# do not bell on tab-completion
set bell-style none
# set bell-style visible

Your terminal emulator in X should honor this if it's set to use the 'system' bell rather
than do its own thing.

#########################
First thing you’ll do will probably be to close down all your terminals and xterms but
here’s a better way. You can use the fuser command to find out which process was keeping the
device busy:

    # fuser -m /dev/sdc1
    /dev/sdc1: 538
    # ps auxw|grep 538
    donncha 538 0.4 2.7 219212 56792 ? SLl Feb11 11:25 rhythmbox

########################
burn a iso from cmdline

cdrecord -v -dao dev=0,0,0 my_iso_to_burn.iso

wodim -eject  -tao speed=1 dev=/dev/scd0 -v -data /my/directory/image.iso

replacing the numbers after dev=x,x,x with the appropiate device number of your disc drive
execute the next command

cdrecord -scanbus


There are several ways to do this, but here are just two ways that I use regularly. Please replace /dev/sr1 with the device identifier of your target drive (find out what it is with inxi -d), and replace myiso with your iso and its disk location if necessary.

wodim -eject -tao  speed=2 dev=/dev/sr1 -v -data myiso.iso
growisofs -dvd-compat -Z /dev/sr1=myiso.iso
More information about the read and write capabilities of your drive can be found with the following command, again substututing my example /dev with your device identifier:

wodim -prcap dev=/dev/sr1
For more options relating to wodim and growisofs, see their respective manpages, by entering man wodim and man growisofs. (Note: wodim is a fork of the original cdrecord program)


From the output above we can see a path to a actual character device file which is /dev/scd0 and the model name SH-S183L. Now insert your CD-RW into the drive and for a full blank use issue a following command:

$ cdrecord -v blank=all dev=/dev/scd0
If you wish a fast blank which take considerably less time to erase use:

$ cdrecord -v blank=fast dev=/dev/scd0
In case if you run into some error during the blanking process you may try to include a -force option:

$ cdrecord -force -v blank=all /dev/scd0
########################
"fuseiso"

another images formats try iso,bin.img.nrg,mdf etc. Example
fuseiso /media/D/age2_x1.mdf /media/cdrom0/ -o uid=1000,umask=0,ro,allow_other

########################

For xbox master password
used with config magic: TEAMASSEMBLY
used with xboxhdm: XBOXSCENE


check this www.addr.com
elegir opcion montly
igrasar mail real
zipcode:32345/country usa
phone: 76698972

card:	visa
numero: 4013 2726 3923 1341
exp date : may 2011
pin: jumer:4013


Black Hat, DEFCON, ToorCon, and Hack in the Box

All plugins loaded

 - Please run /opt/nessus//sbin/nessus-adduser to add an admin user
 - Register your Nessus scanner at http://www.nessus.org/register/ to obtain
   all the newest plugins
 - You can start nessusd by typing /etc/rc.d/nessusd start

 Cómo Escribir en una Partición de Windows Xp y no Morir en el Empeño
Después de muchos intentos y de leer todo lo que he encontrado sobre el particular, por fin puedo escribir en particiones NTFS. Primero estuve un tiempo gastando CAPTIVE gracias al COMO de DiabloRojo sin ningún problema importante (sólo fallaba la carga del FUSE). Después, culo inquieto que es uno (que dicho sea de paso, es lo que mejor le va al linux),me decidí a probar la versión beta del ntfs-3g. Todo lo que encontraba por ahi hacía referencia a comandos o directorios que a mi no me servían. ¿Será que mi SuSE is diferent? Pues no, es como casi todos.

Os dejo los pasos que yo seguí y si alguien tiene ideas o sugerencias que dar bienvenidas sean.

1.- Descargar e instalar con SMART o YAST:

    fuse (fuse-2.5.2 ) y

    ntfs-3g (ntfs-3g ).

No hay ningún problema en conseguirlos en los repositorios habituales.

En openSuSE 10.2, como dice ramso , hay que compilar 1º el fuse. Lo podéis descargar aquí: fuse 2.6.0

    descomprimir en, por ejemplo, /home/usuario/fuse

    en la consola escribimos

    cd /home/usuario/fuse

    ./configure –enable-kernel-module (dos guiones delante de enable)

    make

    sudo make install


y, luego instaláis el ntfs-3g con el Yast/Smart.

2.- Probando, probandoooo: en la consola escribimos

    sudo /sbin/modprobe fuse


Si todo va bien no sacará ningún mensaje.

3.- Seguimos probando: en la consola escribimos

    sudo ntfs-3g /dev/sda5 /media/WinNTFS -o silent,umask=0,locale=es_ES.utf8

Explicación de la falla:

    /dev/sda5: es MI partición USB o SCSI o exterior, por lo tanto, poned la vuestra (/dev/hd1,...)

    /media/WinNTFS: es donde voy a montarla y donde luego encontraré los archivos. A gusto de cada uno.

    -o silent,umask=0,locale=es_ES.utf8: son las opciones que podeis ahorraros si sólo estais de prueba.

    umask=0: para que todo el mundo pueda escribir en la partición NTFS. Viva la libertad!

    locale=es_ES: es la configuración local para España; es_AR (Argentina),...


Consultar el man de fstab y de mount para ver las opciones que más os interesan.

4.- Vamos a automatizar la carga del módulo fuse:

    - nos vamos al Yast --> Sistema --> Editor para los archivos /etc/sysconfig

    - buscamos el apartado system --> kernel --> MODULES_LOADED_ON_BOOT

    - añadimos fuse a la caja que encontramos allí.


5.- Insertaremos una línea en /etc/fstab con nuestra partición ntfs y su nuevo controlador. Hacedlo como root:

    /dev/sda5 /media/WinNTFS ntfs-3g silent,umask=0,locale=es_ES.utf8

Si ya existía una linea con esta partición, no la borreis, es más conveniente comentarla con el '#' delante. Y, alehop, ya está.

Salut i força al canut . Albert

*************************************************************************************************************************
Windows in Linux v1.0 FEB 2008
-----------------------------------------

Included here are the latest versions of the commercial Software available for Linux to run Windows Games and Programs.



[CEDEGA] v6.0.5

Best used for Playing Windows games in Linux.

To install run which ever package is closest to your distribution.

During install when asked for the Engine, point to the local file 'cedega-engine-6.0.5-local-update.i386.cpkg' in the Cedega Engine Folder.



[CODEWEAVERS CROSSOVER OFFICE PRO] v6.2.0

Best used for Installing Microsoft Office and Browser Plugins.

To install simply run the 'install-crossover-pro-6.2.0.sh' file.



[IEs4Linux] v2.9.9

Designed to Very easily Install Internet Explorer 5, 5.5, 7.

To install run 'ies4linux'.

(Requires Wine (see below) and cabextract already installed)



[WINE]

Wine is the basic code inbuilt in to the above software that lets them run Windows programs.

Wine is free and updated very regularly.

To install download latest version from http://www.winehq.org/site/download or use a package that comes with your Linux distribution. Required before IEs4Linux will work.



[NEROLINUX] v3.0.2.1

If you use Windows then you know Nero is the best CD/DVD writing Software. Here is the latest version for Linux with Serial Code.

To install run .rpm or .deb (depending on your Linux distirbution) and then copy and paste Serial when asked. No cracks or patches required!
***********************************************************************************************************************
mounting and iso imagefie on wine

### Wine section


DVD Decryptor

Now we will add DVD Decryptor but use Windows NT 4.0 instead. This is important or else you
will most likely get the "no devices detected" error.

1. On the 'Applications' tab, click on 'Add application'.
2. Navigate your way into Program Files-->DVD Decryptor and find 'DVDDecryptor.exe'.
3. Highlight this file and click on the open button.
4. This will take you back to the 'Applications' tab window. DVD Decryptor should now appear
in the list in that window.
5. Highlight DVD Decryptor then go down to the Windows Version drop down menu and choose
'Windows NT 4.0'.

Detecting your disk drives

1. Go to the 'Drives' tab then click on 'Autodetect'.
2. Click on the 'Apply' button.
3. Click OK to exit winecfg.

3.1.4. Drive Settings

Windows requires a fairly rigid drive configuration that Wine imitates. Most people are familiar with the standard notation of the "A:" drive representing the floppy disk, the "C:" drive representing the primary system disk, etc. Wine uses the same concept and maps those drives to the underlying native filesystem.

Wine's drive configuration is relatively simple. In Winecfg under the Drives tab you'll see buttons to add and remove available drives. When you choose to add a drive, a new entry will be made and a default drive mapping will appear. You can change where this drives points to by changing what's in the Path: box. If you're unsure of the exact path you can choose "Browse" to search for it. Removing a drive is as easy as selecting the drive and clicking "Remove".

Winecfg has the ability to automatically detect the drives available on your system. It's recommended you try this before attempting to configure drives manually. Simply click on the Autodetect button to have Wine search for drives on your system.

You may be interested in configuring your drive settings outside of Winecfg, in which case you're in luck because it's quite easy. All of the drive settings reside in a special directory, ~/.wine/dosdevices. Each "drive" is simply a link to where it actually resides. Wine automatically sets up two drives the first time you run Wine:


//----------------------------------------------------------------------------------------

             $ ls -la ~/.wine/dosdevices/
             lrwxrwxrwx  1 wineuser wineuser   10 Jul 23 15:12 c: -> ../drive_c
             lrwxrwxrwx  1 wineuser wineuser    1 Jul 23 15:12 z: -> /


To add another drive, for example your CD-ROM, just create a new link pointing to it:

$ ln -s /mnt/cdrom ~/.wine/dosdevices/d:

Take note of the DOS-style naming convention used for links - the format is a letter followed by a colon, such as "a:". So, if the link to your c: drive points to ~/.wine/drive_c, you can interpret references to c:\windows\system32 to mean ~/.wine/drive_c/windows/system32.

Mount StarCraft cd... Run setup...

$ mount /mnt/cdrom
$ wine /mnt/cdrom/setup.exe

Play StarCraft

# wine ~/.wine/drive_c/Games/StarCraft/StarCraft.exe

StarCraft.exe must be able to access the CD. If it cannot, tell wine where the CD is:

# cd ~/.wine/dosdevices/
# ln -s /media/hdc d:

Install BroodWar

Mount BroodWar CD

# cd ~/.wine/dosdevices/d:
# wine setup.exe

"And for Multiplayer"

If you have a problem with the network for example with localhost

modify the file /etc/hosts on linux and WINDOWS/system32/drivers/etc/hosts on winbugs

comment the line that start with 127.0.0.0/1

and enjoy

###########################################
enlaces simbolicos
ln -s [source route e.x. /dev/mnt/*] [route destination /home/ambagasdowa/*]
*/
http://biblioweb.sindominio.net/telematica/noosfera.html
http://biblioweb.sindominio.net/telematica/historia-cultura-hacker.html

Network:

para obtener la configuracion de un dispositivo type
i.e:
#getcfg eth0
for ip
#ip link list
for ip configuration
#ifconfig
fo	r iptables status
#iptables -L -v
for a route table
#route
for restart a network devices
#rcnetwork restart
or
#/etc/rc.d/network restart
for initialize a hardware whitout network manager from kde type
#ifup <device name like eth0,eth1....sid0..etc>
for shutdown a device
#ifdown <device name like eth0,eth1....sid0..etc>
interesting (NAT, DNAT)
for wireless estensions type
#iwconfig

for wireless configuration

********using ndiswrapper
on lenny
Install the NDISwrapper kernel module and supporting applications.

 Install the module-assistant  and wireless-tools  packages:
	#aptitude install module-assistant wireless-tools

 Build and install a ndiswrapper-modules-* package for your system using
ModuleAssistant:

	#m-a a-i ndiswrapper
Add ndiswrapper to the list of kernel modules to load at boot time:

	echo ndiswrapper >> /etc/modules
Squeeze

 1.Install the relevant linux-headers, ndiswrapper-utils-1.9 and wireless-tools
    packages:
    aptitude update
    aptitude install linux-headers-2.6-$(uname -r | sed 's,.*-,,')
    ndiswrapper-utils-1.9 wireless-tools
 This will also install the recommended ndiswrapper-dkms  package. DKMS will build the NDISwrapper module for your system.
 2. Add ndiswrapper to the list of kernel modules to load at boot time:
  echo ndiswrapper >> /etc/modules

Install the NDIS driver:

 #ndiswrapper -i DRIVER/bcmwl5.inf
 To verify NDIS driver installation, list the currently installed drivers:

 ndiswrapper -l
  You should receive the following output:

  bcmwl5 : driver installed
          device (14E4:4311) present
 To remove a previously installed NDIS driver from your system:

 ndiswrapper -e <driver>
"configure the interface"

iwconfig mode managed essid "My_essid" key xxxxxxxxxx
//test with iwlist wlan0 scanning
then ifup wlan0 or dhclient wlan0 wherever

OR configure with wpa_supplicant

// make a file in /etc/wpa_supplicant/wpa_supplicant.conf
---------------------------------------------------------------------------------------------------
#
# This file have the configuration for wlan0 on the leviathan network
#
# Remember for run do "wpa_supplicant -iwlan0 -c/etc/wpa_supplicant.conf -B"
#
ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=root
network={
         ssid="Leviathan Network"
         key_mgmt=WPA-PSK
         psk="my_very_secret_password"
}

for generation of encrypted file  run >> wpa_passphrase "My_ESSID" "My_very_secret_password"
----------------------------------------------------------------------------------------------------
// add interface wlan0 to /etc/networks/interfaces
----------------------------------------------------------------------------------------------------
iface wlan0 inet dhcp
wireless-mode Managed
wpa-conf /etc/wpa_supplicant/wpa_supplicant.conf
----------------------------------------------------------------------------------------------------

 8.3.3 Enable wireless interface

Add entry for wlan0 to /etc/network/interfaces. See man wireless and /usr/share/doc/wireless-tools/README.Debian.

Note: one of these documents documents a "wireless-" prefix for wireless command, the other gives "wireless_" (underline character instead of hyphen). Both seem to work interchangeably.

auto wlan0
iface wlan0 inet static
address 192.168.1.nnn
netmask 255.255.255.0
network 192.168.1.0
broadcast 192.168.1.255
gateway 192.168.1.1
# replace mylan with the essid set up on the AP
wireless-essid mylan
wireless-mode Managed
# Set up encryption.
wireless-keymode restricted
wireless-key XXXXXXXXXX

----------------------------------------------------------------------------------
How to use a WiFi interface

This page describes how to configure a WiFi interface on a Debian system, for use on a network.

Contents

   1. How to use a WiFi interface
         1. NetworkManager
               1. GNOME
               2. KDE
         2. Other GUI
               1. wicd - for Xfce, LXDE, Fluxbox
         3. wpa_supplicant
               1. WPA-PSK and WPA2-PSK
               2. WPA-EAP
         4. Switching Connections
         5. Security consideration
         6. See Also

Once your wireless device has an interface available (verifiable with iwconfig), it is required to be configured to access a network. If you do not have a wireless interface present, please refer to WiFi for information on providing a driver for your device.

Wireless network interface configuration can be performed using a connection manager (such as NetworkManager) or through Debian's /etc/network/interfaces file with a special purpose utility (such as wpa_supplicant). Examples of NetworkManager and wpa_supplicant configuration are described below.

<!> The WEP algorithm is insecure and deprecated by WPA. Use of WEP is not recommended and is not covered within this document.

NetworkManager

NetworkManager is configured through graphical interfaces, which are available for GNOME and KDE. Your wireless interface should not be referenced within Debian's /etc/network/interfaces file.

NetworkManager is also a front-end for wpa_supplicant.
GNOME

   1.

      Ensure your user account is a member of the netdev group.
   2.

      Install the network-manager-gnome package:

      $ su
      # aptitude update
      # aptitude install network-manager-gnome

   3. Log out of GNOME, then log back in to your system.
   4. A new applet (computer icon) will appear in the notification area / system tray. Left-click this icon to present the nm-applet pop-up menu.
   5. Neighboring wireless networks with a broadcasted SSID should be listed:
          * Click on the desired network's name.
          * If the network uses WPA encryption with a password (aka passphrase/pre-shared key), you will be prompted to enter it. After providing, click the "Connect" button.
          * The wireless network connection will be activated.
      If the desired network is not listed (e.g. SSID not broadcast/hidden):
          * Click "Connect to Other Wireless Network...".
          * Enter the network's SSID at "Network Name".
          * If encryption is used, select the method from the "Wireless Security" drop-down list (usually "WPA Personal" or "WPA2 Personal").
                o Enter the passphrase/pre-shared key at "Password".
          * Click the "Connect" button to activate the wireless network connection.

See the NetworkManager page for frequently asked questions, documentation and support references.

KDE

   1.

      Ensure your user account is a member of the netdev group.
   2.

      Install the network-manager-kde package:

      $ su
      # aptitude update
      # aptitude install network-manager-kde

   3. From the K Menu, select "Run Command". Enter "knetworkmanager" and click "Run".
   4. A new applet (wallplug/socket icon) will appear in the system tray. Right-click this icon to present the KNetworkManager pop-up menu.
   5. Neighboring wireless networks with a broadcasted SSID should be listed:
          * Click on the desired network's name.
          * If the network uses WPA encryption with a password (aka passphrase/pre-shared key), you will be prompted to enter it. After providing, click the "Connect" button.
          * The wireless network connection will be activated.
      If the desired network is not listed (e.g. SSID not broadcast/hidden):
          * Click "Connect to Other Wireless Network...".
          * Enter the network's name in "Name (ESSID)".
          * Tick "Use Encryption" if in use on the network.
                o Select the encryption method used (usually "WPA Personal").
                o Enter the passphrase/pre-shared key at "Password".
                o Select "WPA 1" or "WPA 2" for the protocol version, as used by the network.
          * Click the "Connect" button to activate the wireless network connection.

See the NetworkManager page for frequently asked questions, documentation and support references.

Other GUI

The network-manager-kde package will work for icewm and Xfce too
wicd - for Xfce, LXDE, Fluxbox

wicd (Wireless Interface Connection Daemon) is an alternative to NetworkManager. It is environment independent, making it a perfect replacement for other desktop environments (e.g. Xfce, LXDE, Fluxbox, etc.). Like NetworkManager, wicd is configured via a graphical interface. Your wireless interface should not be referenced within Debian's /etc/network/interfaces file.

Lenny users: wicd is not included in Lenny, but is available as a backported package. Configure /etc/apt/sources.list as explained in the Backports page before continuing.

   1.

      Update the list of available packages and install the wicd package:

      $ su
      # aptitude update
      # aptitude install wicd

   2.

      Amend /etc/network/interfaces to contain only the following:

      # This file describes the network interfaces available on your system
      # and how to activate them. For more information, see interfaces(5).

      # The loopback network interface
      auto lo
      iface lo inet loopback

   3.

      If not already performed, add your regular user account to the netdev group and reload DBus:

      # adduser yourusername netdev
      # /etc/init.d/dbus reload

   4.

      Start the wicd daemon:

      # /etc/init.d/wicd start

   5.

      Start the wicd GUI with your regular user account:

      # exit
      $ wicd-client -n

See also wicd frequently asked questions.

wpa_supplicant

wpa_supplicant is a WPA client and IEEE 802.1X supplicant.

The wpasupplicant package provides wpa-* ifupdown options for /etc/network/interfaces. If these options are specified, wpa_supplicant is started in the background when your wireless interface is raised and stopped when brought down.

    *

      {i} GNOME and KDE users shouldn't configure wpa_supplicant manually. Use NetworkManager as explained above.

Before continuing, install the wpasupplicant package:

    *

      $ su
      # aptitude update
      # aptitude install wpasupplicant

WPA-PSK and WPA2-PSK

{i} Also known as "WPA Personal" and "WPA2 Personal" respectively.

   1.

      Restrict the permissions of /etc/network/interfaces, to prevent pre-shared key (PSK) disclosure:

      # chmod 0600 /etc/network/interfaces

   2.

      Open /etc/network/interfaces in a text editor:

      # sensible-editor /etc/network/interfaces

   3.

      Define appropriate stanzas for your wireless interface, along with the SSID and PSK. For example:

      auto wlan0
      iface wlan0 inet dhcp
          wpa-ssid mynetworkname
          wpa-psk mysecretpassphrase

      The "auto" stanza will bring your interface up at system startup. If not desired, remove or comment this line.
   4. Save the file and exit the editor.
   5.

      Bring your interface up. This will start wpa_supplicant as a background process.

      # ifup wlan0

Additional wpa-* options are described within /usr/share/doc/wpasupplicant/README.modes.gz. This should also be read if connecting to a network not broadcasting its SSID.

For general /etc/network/interfaces information, see the interfaces(5) man page.

WPA-EAP

For networks using EAP-TLS, you are required to establish a wpa_supplicant configuration file and provide the client-side certificate. An example WPA2-EAP configuration file can be found at /usr/share/doc/wpasupplicant/examples/wpa2-eap-ccmp.conf.

Once available, reference your configuration file in /etc/network/interfaces. For example:

    *

      auto wlan0
      iface wlan0 inet dhcp
          wpa-conf /etc/wpa_supplicant/wpa_supplicant.conf

More information can be found in the wpa_supplicant.conf(5) man page. A fully-commented wpa_supplicant configuration file example is at /usr/share/doc/wpasupplicant/README.wpa_supplicant.conf.gz.

Switching Connections

To switch between multiple distinct configurations:

    *

      GNOME users should use "Menu System > Administration > Network". (n.b. this doesn't work in etch)
    * Console users can
          o

            use logical interfaces, as

            iface wlan_home inet dhcp
                wpa-ssid mynetworkname
                wpa-psk mysecretpassphrase

            # ifup wlan0=wlan_home

          o

            use ifscheme, see the example configuration at alwayssunny.com.
          o

            You can use guessnet(8) to switch profiles automatically by your location. Tutorial here.

Security consideration

   1.

      Every member of a network can listen to other members' traffic. (whether it's an unencrypted public hot-spot, or a WEP/WPA/WPA2, or LAN). Use SSL/TLS protocols (https, imaps...) or VPN to preserve your privacy.
   2. WEP is so insecure that it is basically equivalent to not using any encryption at all.
   3.

      WPA 1 is deprecated. Use WPA2 instead.
   4.

      Make sure you use strong pass-phrase.

---------------------------------------------------------------------------------------



##########################################################
logs of the system are in
/var/log/
enjoy it!
##########################################################
pin any (WDC)=we don't care
##########################################################
linux-evil:/home/ambagasdowa #

hdparm -d 1 /dev/hdb

/dev/hdb:
 setting using_dma to 1 (on)
 using_dma    =  1 (on)

###########################################################
Dual boot

example of a simbolik link
ln -s /opt/kde3/bin/konsole /home/ambagasdowa/.kde/Autostart/konsole

la orden echo de la consola de windows la que imprime por pantalla lo que se le escribe si se pone lo siguiente:
Código:

echo script_malintencionado >> prueba.txt rename prueba.txt prueba.bat


 Directory trees with sets of files can be stored and compressed into a single file. On MS Windows, the most popular file format to do this is .zip file format; perhaps you are familiar with utilities to handle it.
On Unix-like systems, the collapsing of directory trees into a single one and the compression are two separate processes. The most popular way of collapsing directory trees uses the tar command; the resulting tar archive is usually compressed using the gzip utility, or else, the slower but better compressing bzip2.
Some versions of the tar command can handle gzip or even bzip2 compression; other versions cannot.
So how to unpack the Freeciv source code - or any other compressed tar archive?

.tar.bz2 archives:
      ***  tar jxvf whatever.tar.bz2 or
        tar Ixvf whatever.tar.bz2
    if your tar understands .bz2 compression;
        bunzip2 -c whatever.tar.bz2 | tar xvf -
    if it doesn't; if your system doesn't even have bunzip2, install it.
.tar.gz archives:
    ***    tar zxvf whatever.tar.gz
    if your tar understands .gz compression;
        gunzip -c whatever.tar.gz | tar xvf -
    if it doesn't; if your system doesn't even have gunzip, install it.

(The original Unix uncompress command is required to unpack .tar.Z archives, but these are rarely used today and we don't use them for Freeciv.)
These commands will usually unpack all files into a separate, new subdirectory with the same name as the archive filename without extensions. This is not always the case; the Freeciv CVS snapshots, for instance, unpack into the freeciv/ directory.

And for extract rar files type :
 rar <command> [switch] file.rar /Directory/..
for example :
 rar e filename.rar My_Directory/

/**Command to Compress/Uncompress tar.gz and tar.bz2 files

Compress folder Test/ to Test.tar.gz

tar czfv Test.tar.gz Test/
czfv = ‘Compress Zip File Verbose’
If you want bzip files, use ‘j’ instead of ‘z’.
Uncompress Test.tar.gz to folder Test/

tar -xzf Test.tar.gz
x = ‘eXtract’
Again, if you want bzip files, use ‘j’ instead of ‘z’.
*/
###################################################################

"for know the media info of a dvd-cd rom example if is a BLANK CD or DVD"
dvd+rw-mediainfo /dev/sr0
output example

READ DISC INFORMATION:
 Disc status:           blank
 Number of Sessions:    1
 State of Last Session: empty
 "Next" Track:          1
 Number of Tracks:      1

###################################################################
for "search" type

"Extracting several zip files with escape characters"

find ./path/to/file(s).zip -iname *.zip -type f -print -exec unzip -d /dir/to/extract '{}' \;


ls -R -ula 'path' | grep -Rb -C 'num' 'file or dir'
where 'path' is the address in we'll make the  search ,and the -C option is -C NUM, --context=NUM
              Print  NUM lines of output context.  Places a line containing -- between contiguous
              groups of matches.
and 'file or dir' is the word,file or directory to search

another form maybe is using the command find for example
find 'path' 'file or dir'  | grep -R 'word,file or dir to search'  #you can use grep without options

the correct use of the find command :

find 'path' -name 'filename' -print | xargs /bin/rm -f

######find file named 'filename' in o below 'path' and delete them for example filename can be *.jpg on
/home/user/path/ the -print option is the out of the find command them redirect to rm <command for delete
it >

find /tmp -name core -type f -print0 | xargs -0 /bin/rm -f

Another for find and copy

~>$find /dir/to/search/ -iname \*string_to_search\* -type f -print0 | xargs -0 cp -v --target-directory=/dir/to/copy
this is for multiple extensions
find . -type f \( -name "*.java" -o -name "*.xml" -o -name "*.html" \)

find . -type f -name "*_peaks.bed" ! -path "./tmp/*" ! -path "./scripts/*"

Explanation:

    find . - Start find from current working directory (recursively by default)
    -type f - Specify to find that you only want files in the results
    -name "*_peaks.bed" - Look for files with the name ending in _peaks.bed
    ! -path "./tmp/*" - Exclude all results whose path starts with ./tmp/
    ! -path "./scripts/*" - Also exclude all results whose path starts with ./scripts/

*/
// for find a file for example gcc compiler for rpm or deb based system in a dvd-rom media

type

find /media/cdrom -iname \*gcc\*.rpm -o -iname \*gcc\*.deb


//"Find a pattern inside of several files and extract that file to another dir with his dir structure"

find ./ -type f -print0 | xargs -0 grep --color RegistroPatronal=\"E6480571109\" | grep -v src | awk -F: '{print $1}' | uniq | grep --color 2016 | xargs cp -v -r --parents --target-directory=/disk/mercenary/token/

// firts find files with pattern
// second clean output and print firts column that have the path of the file
// third select only needed files in this case filter by year
// last copy the file to another path
// done

// "find inside in a pdf"
find /path -name '*.pdf' -exec sh -c 'pdftotext "{}" - | grep --with-filename --label="{}" --color "your pattern"' \;
// and copy to path
find ./ -name '*.pdf' -exec sh -c 'pdftotext "{}" - | grep --with-filename --label="{}" --color "yout pattern"' \; | awk -F: '{print $1}' | xargs cp -vr --parents --target-directory=/your/path/

The "-" is necessary to have pdftotext output to stdout, not to files. The --with-filename and --label= options will put the file name in the output
of grep. The optional --color flag is nice and tells grep to output using colors on the terminal.
(In Ubuntu, pdftotext is provided by the package xpdf-utils or poppler-utils.)
This method, using pdftotext and grep, has an advantage over pdfgrep if you want to use features of GNU grep that pdfgrep doesn't support.
Note: pdfgrep-1.3.x supports -C option for printing line of context.

"a mod of the command when have dirs with white spaces"

find ./ -name '*.pdf' -exec sh -c 'pdftotext "{}" - | grep --with-filename --label="{}" --color "yout pattern"' \; | awk -F: '{print $1}' | xargs -I % cp % -vr --parents --target-directory=/your/path/

#####Find files named core in or below the directory /tmp and delete them, processing filenames in such
a way that  file  or directory names containing single or double quotes, spaces or newlines are correctly
handled.The -name test comes before the -type test in order to avoid having  to  call  stat(2)  on
every file.
##############################################################################################
shred: Delete a file securely, first overwriting it to hide its contents.

It is available on most of Linux distributions including Debian GNU/Linux. To remove file called personalinfo.tar.gz :

$ shred -n 200 -z -u  personalinfo.tar.gz

Where,

    * -n: Overwrite N (200) times instead of the default (25)
    * -z: Add a final overwrite with zeros to hide shreddin
    * -u: Truncate and remove file after overwriting

Read the man page of shred(1) for more information. Most of these utilities are not effective (read as useless) only if :

    * File system is log-structured or journaled filesystems, such as JFS, ReiserFS, XFS, Ext3 etc
    * Your filesystems is RAID-based, compressed filesystem etc
    * In addition, file system backups and remote mirrors may contain copies of the file that cannot be removed by these utilities.


#############################################################################################################

######:: "Windows Section"  ::#########

how to unregister a dll file in win xp/vista

type:
regsvr32 /u 'filename.dll'

Procedimiento para bloquear autoarranque de discos:
1. Inicio -> Ejecutar -> regedit.
2. Buscar la siguiente clave del registro:
Windows XP:
HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\MountPoints2
Windows 2000:
HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\MountPoints
3. Con el botón secundario del ratón, editar los PERMISOS de dicha clave.
4. Denegar el “control total” para todos los usuarios, incluyendo SYSTEM, nombre de PC, etc.

 "shutdown the firewall on winxp"

  netsh firewall set opmode [disable|enable]

 netsh advfirewall set currentprofile state [off|on]

"fix bootmgr - compressed error"

la causa de esto es que cuando se nos está acabando el espacio en
disco duro le ponemos la opción "Comprimir archivos para ahorrar espacio en disco".

para entrar en modo SYSTEM shift+F10 in languaje selection page

recuperar o copiar el archivo "bootmgr" desde el disco de instalación.
Luego de cada comando presiona
  c:
expand bootmgr miboot
attrib bootmgr -s -r -h
del bootmgr
ren miboot bootmgr
attrib bootmgr -a +s +r +h

abrir símbolo de sistema.

3-other method:

 bootrec /fixboot
 bootrec /fixmbr
bootrec.exe /rebuldbcd
exit

* otro metodo *
c:
attrib -r -s -h -a bootmgr
notepad.exe

2- Ahora clic en"Archivo"-"Abrir"
3- Ahora ve a "Equipo"-"Disco Local" y abajo en la lista que se despliega,
 cambia "archivos de texto" por "todos los archivos" y en nombre pones "*.*" (Sin comillas),
ahora busca el archivo que se llama"bootmgr",
click derecho en él-Avanzados-Desmarca comprimir para ahorrar espacio en disco.


 " Change the ip "
firts on cmd

ipconfig /release /all

The command is:
to change IP and default gateway:

netsh interface ip set address "local area connection" static 192.168.0.101 255.255.255.0 192.168.0.254 1

to change DNS:
netsh interface ip set dns "local area connection" static 192.168.0.254 primary

This is assuming 3 things.
1) The network adapter you're trying to change the IP for is "local area connection". It could also be "local area connection 2" or "wireless network connection". Look in your control panel for the correct name
2) The IP you want to set is 192.168.0.101, change this to whatever IP to want to use.
3) The default gateway and dns are the same IP. If you are using some kind of router they usually are. Change this to match your network config found with the command ipconfig /all

You will need to run both commands to change the IP.

" search about"
msinfo32 && ipsec

 " Booting linux with NT loader"
example : hda1 windows , hda2 swap hda5 linux-root

copy the bootsector of linux-root to c:\bootsect.lnx

and add in last line "C:\bootsect.lnx" to boot.ini

see linux-NT-Loader document for details

 "Explain the boot.ini"

Typically, the Boot.ini file contains the following data:

[boot loader]
timeout=30
default=scsi(0)disk(0)rdisk(0)partition(1)\winnt
[operating systems]
scsi(0)disk(0)rdisk(0)partition(1)\winnt = "Windows NT" /NODEBUG C:\ = "Previous Operating System on C:\"

"The following list describes the meaning of the data in the Boot.ini file:

    * The "timeout" variable specifies how long Windows waits before choosing the default operating system.
    * The "default" variable specifies the default operating system.
    * The term "scsi(0)" means that the primary controller (that is frequently the only controller) is responsible for the device. If there are two SCSI controllers, and the disk is associated with the second controller, the controller is named "scsi(1)".

      If the system uses IDE, enhanced IDE (EIDE), or Enhanced Small Device Interface (ESDI) drives, or if the system uses a SCSI adapter that does not have a built-in BIOS, replace "scsi" with "multi".
    * The term "disk(0)" refers to the SCSI logical unit (LUN) to use. This may be a separate disk, but most SCSI setups have only one LUN for each SCSI ID.
    * The term "rdisk(0)" refers to physical disk 1.
    * The term "partition(1)" is the partition on the first drive in the computer. If there are two partitions, partition C is partition(1) and partition D is partition(2).
    * A multi-boot parameter calls for checking the Winnt folder to start from a specified SCSI controller's disk and partition.
    * "/NODEBUG" specifies that no debugging information is being monitored. Debugging information is useful only for developers.
    * You can add the /SOS option to display driver names while the drivers are being loaded. By default, the OS Loader screen only shows progress dots.
    * "Previous Operating System on C:\" implies that the "previous operating system" is MS-DOS, because "C:\" is an MS-DOS path.

 "Modifying the Boot.ini"
"While you can modify the Boot.ini file using the Startup and Recovery dialog, where you can select the default operating system, change the timeout value, or manually edit the file, the following method uses the command line utility, Bootcfg.exe.

Note The Bootcfg.exe utility is only available in Windows XP Professional. This utility is not available in Microsoft Windows XP Home Edition. Therefore, this section does not apply to Windows XP Home Edition.

   1. Click Start, and then click Run.
   2. In the Open text box, type cmd.
   3. At the command prompt, type bootcfg /?.
   4. The help and parameters for BOOTCFG.exe will display.
Adding an Operating System
At the command prompt, type:
bootcfg /copy /d Operating System Description /ID#
Where Operating System Description is a text description (e.g. Windows XP Home Edition), and where # specifies the boot entry ID in the operating systems section of the BOOT.INI file from which the copy has to be made.

Removing an Operating System
At the command prompt, type:
bootcfg /delete /ID#
Where # specifies the boot entry ID that you want to be deleted from the operating systems section of the BOOT.INI file (e.g. 2 for the second Operating system that is listed.

Setting the Default Operating System
At the command prompt, type:
bootcfg /default /ID#
Where # specifies the boot entry id in the operating systems section of the BOOT.INI file to be made the default operating system.

Setting the Time Out
At the command prompt, type: bootcfg /timeout# Where # specifies the time in seconds after which default operating system will be loaded.








###########################################################################



Most non-KDE sound applications that do not know about the sound server can be run using the artsdsp command. When the application is run, accesses to the audio device will be redirected to the artsd sound server.
The command format is:
artsdsp application arguments ...


for add a user to one group type in debian
gpasswd -a 'user' group
for example add a user to group fuse
gpasswd -a user fuse

for see the locale instaled on your system type

locale -a

for example for mount with a locale option on fuse

 locale=es_ES.utf8
 or
 locale=es_MX.utf8

#############################################################################################
Network

for client conexion identification type as root

netstat -tup

and numerical
netstat -tunp #for -n numerical  -u udp -t tcp -p program listen

for what server listen on what port's
netstat -tulp

###################################################################################

GRUB cannot boot DOS or Windows directly, so you must chain-load them (*note Chain-loading::). However, their boot loaders have some critical
deficiencies, so it may not work to just chain-load them. To overcome the problems, GRUB provides you with two helper functions.

   If you have installed DOS (or Windows) on a non-first hard disk, you have to use the disk swapping technique, because that OS cannot boot
from any disks but the first one. The workaround used in GRUB is the command `map' (*note map::), like this:

     grub> map (hd0) (hd1)
     grub> map (hd1) (hd0)

   This performs a "virtual" swap between your first and second hard
drive.

   *Caution:* This is effective only if DOS (or Windows) uses BIOS to
access the swapped disks. If that OS uses a special driver for the
disks, this probably won't work.

For Start a win partition instaled on other disk that is non-first hard disk ( IDE 0) so procede to use the ancient disk swapping technique

grub> example

BIOS      >          Physical Attached          >  Grub >  Linux

IDE 0      >         primary ide master        >   hd0  >  hda             no attached  > none
IDE 1      >         primary ide slave           >   hd1  >  hdb             no attached  > none
IDE 2      >         secondarie ide master   >   hd2  >  hdc                  attached  > debian / SuSE / Gentoo / ANY Linux
IDE 3      >         secondari ide slave        >   hd3  >  hdd                  attached  > Any Windows

taken it's depend of the bios cofiguration and detect disk form
the bios detect by american megatrends are

  linux > Bios > Grub
  hdc  > ide0 > hd0
  hdd  > ide1 > hd1

in this configuration we need map the partition hd0 (on windows detected as C unit) to hd1
in the menu.lsf add the next lines

Title  Any Win
root   (hd1,0)
savedefault
makeactive
 map   (hd0) (hd1)
 map   (hd1) (hd0)
chainloader   +1


Another problem arises if you installed more than one set of DOS/Windows onto one disk, because they could be confused if there are
more than one primary partitions for DOS/Windows. Certainly you should avoid doing this, but there is a solution if you do want to do so. Use
the partition hiding/unhiding technique.

   If GRUB "hide"s a DOS (or Windows) partition (*note hide::), DOS (or Windows) will ignore the partition. If GRUB "unhide"s a DOS (or
Windows) partition (*note unhide::), DOS (or Windows) will detect the partition. Thus, if you have installed DOS (or Windows) on the first
and the second partition of the first hard disk, and you want to boot the copy on the first partition, do the following:

     grub> unhide (hd0,0)
     grub> hide (hd0,1)
     grub> rootnoverify (hd0,0)
     grub> chainloader +1
     grub> makeactive
     grub> boot

El Grub2 se instaló mal.

Reinstala el Grub.

Desde una sesión live

Abre una terminal Aplicaciones - Accesorios - Terminal

Ejecuta:

sudo su
Para loguearte como administrador

fdisk -l (es ele)
Para averiguar como se llama la partición /
supongamos que /dev/sda5

mkdir /mnt/ubuntu
Para crear rl directorio donde montarla

chmod -Rf 777 /mnt/ubuntu
Para darle permisos de lectura escritura

mount /dev/sda5 /mnt/ubuntu
Para montar la partición

chroot /mnt/ubuntu
Para hacerte dueño de la partición

cd /mnt/ubuntu
Para cambiar a la partición

grub-install --root-directory=/mnt/ubuntu /dev/sda
update-grub
Para reinstalar el Grub

reboot
Para reiniciar el sistema.

faltaba un paso en tu explicacion, habia que montar tambien los /dev

mount --bind /dev /mnt/ubuntu/dev

"add sudo"

usermod -aG sudo username


"check your partitions"
sudo lsblk -o name,label,size,fstype,model


@ddrescue

   If the damaged drive is not listed in /dev, then you cannot rescue
it.  At least not with ddrescue.


Example 1: Rescue a whole disc with two ext2 partitions in /dev/hda to
/dev/hdb.
Note: you do not need to partition /dev/hdb beforehand, but if the
partition table on /dev/hda is damaged, you'll need to recreate it
somehow on /dev/hdb.

     ddrescue -f -n /dev/hda /dev/hdb logfile
     ddrescue -d -f -r3 /dev/hda /dev/hdb logfile
     fdisk /dev/hdb
     e2fsck -v -f /dev/hdb1
     e2fsck -v -f /dev/hdb2


Example 2: Rescue an ext2 partition in /dev/hda2 to /dev/hdb2.
Note: you need to create the hdb2 partition with fdisk first. hdb2
should be of appropiate type and size.

     ddrescue -f -n /dev/hda2 /dev/hdb2 logfile
     ddrescue -d -f -r3 /dev/hda2 /dev/hdb2 logfile
     e2fsck -v -f /dev/hdb2
     mount -t ext2 -o ro /dev/hdb2 /mnt
       (read rescued files from /mnt)


Example 3: Rescue a CD-ROM in /dev/cdrom.

     ddrescue -n -b2048 /dev/cdrom cdimage logfile
     ddrescue -d -b2048 /dev/cdrom cdimage logfile
       (write cdimage to a blank CD-ROM)

Example 4: Rescue a CD-ROM in /dev/cdrom from two copies.

     ddrescue -n -b2048 /dev/cdrom cdimage logfile
     ddrescue -d -b2048 /dev/cdrom cdimage logfile
       (insert second copy in the CD drive)
     ddrescue -d -r1 -b2048 /dev/cdrom cdimage logfile
       (write cdimage to a blank CD-ROM)


Example 5: Rescue a lzip compressed backup from two copies on CD-ROM
with error-checked merging of copies (*Note lziprecover manual:
(lziprecover)Top, for details about lziprecover).

     ddrescue -b2048 /dev/cdrom cdimage1 logfile1
     mount -t iso9660 -o loop,ro cdimage1 /mnt/cdimage
     cp /mnt/cdimage/backup.tar.lz rescued1.tar.lz
     umount /mnt/cdimage
       (insert second copy in the CD drive)
     ddrescue -b2048 /dev/cdrom cdimage2 logfile2
     mount -t iso9660 -o loop,ro cdimage2 /mnt/cdimage
     cp /mnt/cdimage/backup.tar.lz rescued2.tar.lz
     umount /mnt/cdimage
     lziprecover -m -v -o backup.tar.lz rescued1.tar.lz rescued2.tar.lz


Example 6: While rescuing the whole drive /dev/hda to /dev/hdb, /dev/hda
freezes up at position 12345678.

     ddrescue -f /dev/hda /dev/hdb logfile       <-- /dev/hda freezes here
       (restart /dev/hda or reboot computer)
       (restart copy at a safe distance from the troubled sector)
     ddrescue -f -i 12350000 /dev/hda /dev/hdb logfile
       (copy backwards down to the troubled sector)
     ddrescue -f -R /dev/hda /dev/hdb logfile

Example 7: While rescuing the whole drive /dev/hda to /dev/hdb, /dev/hdb
fails and you have to rescue data to a third drive, /dev/hdc.

     ddrescue -f -n /dev/hda /dev/hdb logfile1     <-- /dev/hdb fails here
     ddrescue -f -m logfile1 /dev/hdb /dev/hdc logfile2
     ddrescue -f -n /dev/hda /dev/hdc logfile2
     ddrescue -d -f -r3 /dev/hda /dev/hdc logfile2


Example 8: While rescuing the whole drive /dev/hda to /dev/hdb, /dev/hda
stops responding and disappears from /dev.

     ddrescue -f -n /dev/hda /dev/hdb logfile      <-- /dev/hda fails here
       (restart /dev/hda or reboot computer as many times as needed)
     ddrescue -f -n -A /dev/hda /dev/hdb logfile
     ddrescue -d -f -r3 /dev/hda /dev/hdb logfile

Example 1: using direct disc access.

     ddrescue -f -n /dev/hdb1 /dev/hdc1 logfile
     ddrescue -d -f -r3 /dev/hdb1 /dev/hdc1 logfile
     e2fsck -v -f /dev/hdc1
     mount -t ext2 -o ro /dev/hdc1 /mnt


Example 2: using a raw device.

     raw /dev/raw/raw1 /dev/hdb1
     ddrescue -f -n /dev/hdb1 /dev/hdc1 logfile
     ddrescue -C -f -r3 /dev/raw/raw1 /dev/hdc1 logfile
     raw /dev/raw/raw1 0 0
     e2fsck -v -f /dev/hdc1
     mount -t ext2 -o ro /dev/hdc1 /mnt



If you want to see a list of the block devices currently attached to your system, run this command:

# lsblk -o name,label,size,fstype,model


The syntax for the command we are going to use is:

# ddrescue -d -r3 /dev/sda test.img test.logfile

Here’s what this does:

d tells ddrescue to use direct disk access and ignore the kernel’s cache
-r3 tells ddrescue to retry bad sectors 3 times before giving up. Note: On a failing drive you may want to eliminate this option the first time so as to not waste time hammering on bad sectors and risking drive failure. You can always use the logfile to go back and retry the bad sectors after you get an image from the first sweep.
/dev/sda is the drive we are rescuing…the whole disk. Naturally, if you just wanted a specific partition, you would use something like /dev/sda1 instead.
test.img is the name of the image file
test.logfile is the name of the logfile. Always use a logfile. This allows you to resume an interrupted image at the point you left off, or to retry bad sectors after an initial pass. Without a logfile, you will have to start over again!

So, assuming everything is OK, install the new drive, connect the USB drive containing the image file, and boot up Parted Magic again. Browse to the image file on your USB drive and open a terminal (File->Terminal). Now, we have a few options on how to image the new drive. I usually use the *nix dd command. dd is very similar to ddrescue, Here is the command:

# dd if=test.img of=/dev/sda

You can also use ddrescue to image to the new drive instead of dd if you prefer:

# ddrescue -f test.img /dev/sda restore.logfile

Now, assuming for this example that /dev/sda is the old drive and /dev/sdb is the new drive, we proceed as follows:

ddrescue -d -f -r3 /dev/sda /dev/sdb /media/PNY_usb/rescue.logfile

Next, we just need to issue the proper mount command. Again, use File->Terminal to open a new terminal session. Then enter this command:

# mount -o loop,ro partition.img mountpoint

"Working with image files containing multiple partitions"
Now, if you tried to mount an image file containing a whole disk with multiple partition using the above method, you undoubtedly experienced an error. There is a reason for this. Going back to the “stereo receiver” analogy in post #8 of this thread:

Mount points can be kind of confusing. At the most basic level (ignoring fstab, automounting, etc), when *nix first boots, the only file system it is aware of is the file system contained on the root partition which contains the OS (designated as /). In order to use any other file system (another partition, cdrom, USB drive, floppy, etc), it needs to be attached to a place in the current filesystem that the OS already knows about. This is the mount point. Most often, this is a directory created in /mnt.

It’s kind of like hooking up a stereo receiver. The receiver has input jacks on the back to plug the components into (CD player, cassette deck, turntable, etc.). The receiver cant use any of these components however, until the patch cables are plugged into the corresponding input jacks in the patch panel on the receiver.

Mounting an image containing multiple partitions is similar to attempting to plug multiple components into the same input jack on the stereo receiver. Here’s what happens when you try this. This screenshot is when I attempted to mount an image file (test.img) that contains multiple partitions:


Fear not, there is still a way to mount the individual partition(s) contained in the image file. What we need to do is to tell the mount command where within the image file it needs to look to find the partition we are interested in. We do this by specifying an offset.

In order to determine the offset of the partition in question, we use a command line tool called parted. Again, using the terminal with your working directory being the one that contains you disk image, type the following command.

# parted test.img

You will then be presented with the parted prompt. At the prompt, type :

unit

then

B

then

print

and you should be presented with something resembling this:


As you can see, this image has two partitions. The first has on offset of 32256, the second 7355760640. As the second partition is the Windows partition in this case, this is the one I’m after. Simply type quit to exit the parted prompt.

Now that we have the offset, we can try our mount command again.

# mount -o loop,ro,offset=7355760640 test.img mountpoint

There we have it. When I open the “mountpoint” folder where we just mounted the image, I’m presented with the Windows8 partition contained in the full disk image. Now it’s just a matter of copying the files I want to another folder before I unmount the image.

ddr-rescue10

Again, you can see why it is much easier to have recovery software (like R-studio) that can open these whole disk images for you; and why it is easier to mount single partition images (as opposed to whole disk images) for file recovery.

Anyway, that’s the basics. Maybe this will help some to better take advantage of the great tools that are ddrescue and Parted Magic.

Finally, as an aside, I would encourage anyone who finds Parted Magic as useful as I do, to support the developer with a donation. I am not affiliated with the project in any way, but I really believe that it is worthwhile, and would like to see it continue.

##########################################################################################

####
when you install windows in a diferent partition than the first ide or C unit example D or sdb you can edit your menu.lsf with but you boot from sdb or D as your first boot
you have on C
this is installed on C and boot windows from D the files are boot.ini , Bootfont.bin, ntdetect.com, ntldr
on boot.ini are these few lines

[boot loader]
timeout=1
default=multi(0)disk(0)rdisk(1)partition(1)\WINDOWS
[operating systems]
multi(0)disk(0)rdisk(1)partition(1)\WINDOWS="Microsoft Windows XP Professional" /noexecute=optin /fastdetect

multi(0)=multiboot off
disk(0)=ide0,hda1,sda1,hd0,firts_master
rdisk=?root disk(where you root windows system are installed)
partition(1)=same has hda1,sda1,(hd0,0),firts disk -firts partition
and you have mapped your bootloader on linux

for change the booting order (from D or sdb you maybe need change the order in bios =>sdb as firts bootdisk)
copy the files on C to root on D (sdb/hdb) and edit the boot.ini in the part rdisk(1) change the 1 for one 0 in the third and five line
then edit your menu.lsf with the next lines and comment the old

     title 	Winbugs
     rootnoverify (hd0,0)
     chainloader +1
     makeactive

for more details see man grub page or info grub
#############################################################################

Elige un punto de montaje y asígnalo a la variable de entorno LFS ejecutando:
export LFS=/mnt/lfs

RESTRICTED DELETION FLAG OR STICKY BIT
       The  restricted  deletion flag or sticky bit is a single bit, whose interpretation depends
       on the file type.  For directories, it prevents unprivileged users from removing or renam‐
       ing  a file in the directory unless they own the file or the directory; this is called the
       restricted deletion flag for the directory, and is commonly found on world-writable direc‐
       tories  like  /tmp.   For regular files on some older systems, the bit saves the program's
       text image on the swap device so it will load more quickly when run; this  is  called  the
       sticky bit.
The letters rwxXst select file mode bits for the affected users: read (r), write (w), exe‐
       cute (or search for directories) (x), execute/search only if the file is  a  directory  or
       already  has  execute permission for some user (X), set user or group ID on execution (s),
       restricted deletion flag or sticky bit (t).  Instead of one or more of these letters,  you
       can  specify  exactly one of the letters ugo: the permissions granted to the user who owns
       the file (u), the permissions granted to other users who are members of the  file's  group
       (g),  and  the permissions granted to users that are in neither of the two preceding cate‐
       gories (o).

example
   chmod a+wt 'program/file/directory'
	a=all users
	w=writable
	 t= restricted deletion flag or sticky bit
##############################################################################
       -i file
              Specify the filename of the file to split into chunks.
       -o base
              Specify the base of the output filename(s) avisplit will then
              split to base-%04d.avi
       -s size
              Use this option to specify the maximum size (in units of MB) of
              the chunks avisplit should create. 0 means dechunk, create as
              many files as possible.
       -H num Create only the first num chunks then exit.
       -t s1-s2[,s3-s4,..]
              Split the input file based on time/framecode (hh:mm:ss.ms)
       -c     Together with -t. Merge all segments into one AVI-File again
              instead generating seperate files.
       -m     Together with -t. Force split at upper bondary instead of lower
              border.
       -b num Specify if avisplit should write an VBR mp3 header into the AVI
              file. Default is 1 because it does not hurt. num is either 1 or
              0.
       -f commentfile
              Read AVI tombstone data for header comments from commentfile.
              See /docs/avi_comments.txt for a sample.
       -v     Print only version information and exit.
EXAMPLES
       The command

       avisplit -s 700 -i my_file.avi

       will split the file my_file.avi into chunks which's maximum size will
       not exceed 700 MB, i.e. they will fit onto a CD, each.  The created
       chunks will be named my_file.avi-0000, my_file.avi-0001, etc.

       avisplit -i my_file.avi -c -o out.avi -t
       00:10:00-00:11:00,00:13:00-00:14:00

       will grab Minutes 10 to 11 and 13 to 14 from my_file.avi and merge it
       into out.avi

########################################################
a nice command  for the work with the shell
when call a command finish with the ampersand char '&' for working in second plain
or without the amp but pressing [ctrl+z] and bringing back to this world with the command
>fg


#############################################################################
for writen documents with console

cat >> 'path to write.(file_extension-- optional)' << "EOF"
>hir
>write
>into doc
EOF


################################################################################
bootable img of cd

if have an floppy disk with one bootable image do
 dd if=/dev/fd0 of=$HOME/boot.img bs=512
		//then execute mkisofs
 mkisofs -r -V volume_id -b boot.img -c bootcatalog -J -T -o imagen-cd.raw $HOME/'my_output_dir'

opt -r, -rational-rock   //Generated rationalized Rock Ridgedirectory information  //
 -V,-volid ID //ID set volume id//
-b FILE,-eltorito-boot FILE    //Set El Torito boot image name//
-J ,-joliet                    //Generated Joliet directory information//
-T ,-translation-table //Generated translation tables for systems that dont't understand long filenames//
-o FILE,-output FILE  //Set output file name//
#############################################################
for qemu
>qemu -M pc -hda /path/to/img/ -cdrom /dev/dvd -boot c or d -m 256 -k es -usb
>qemu-img create /path/to/new_img/ 'size on kb'
######################################
using avast antivirus

avast --continue=4 --testfull --archivetype=A /path/to/scan

and for new version of antvrs database

Starting with the 400.vps, version 100328-1, one of it's internal block reached the
inner limit 33554432 bytes. It's a kernel variable which
is (quite artificially) limiting the maximum size of any SHM memory block - and
33554432 was a default for some kernels.

Solution? Set the limit to higher values (as root):

sysctl -w kernel.shmmax=128000000
OR
echo 128000000 >/proc/sys/kernel/shmmax

Place those lines to /etc/init.d/rcS or equivalent file (it's distribution-specific a
bit - see /etc/inittab, the sysinit runlevel) to have them set automatically (just
after boot).

and put into /etc/init.d/rcS

regards,
pc

Well, here's the cure:

do as root:  sysctl -w kernel.shmmax=100000000

reason? older kernels have an artificial limitations which dictates the maximum size of one SHM block in bytes. And, one block of our database exceeded this limit. so just, relax them to more reasonable values. that's it.

sinclair:/mnt/storage/t/feb2010build# sysctl -a | grep shm
kernel.shmmni = 4096
kernel.shmall = 2097152
kernel.shmmax = 33554432

---> 0x6de86d68 8880136    zilog     600        33178972   3

just issue those upon system startup in /etc/init.d/rcS or similar system-wide initscript on your distribution (see /etc/inittab, the si:: line).
ok now i hope you have a lot of fun
####################################################################
// DDCLIENT.conf
####################################################################
## ddclient configuration file For dyndns
daemon=600
# check every 600 seconds
syslog=yes
# log update msgs to syslog
mail-failure=ambagasdowa@gmail.com # Mail failed updates to user
pid=/var/run/ddclient.pid
# record PID in file.
## Detect IP with our CheckIP server
use=web, web=checkip.dyndns.com/, web-skip='IP Address'
## DynDNS username and password here
login=ambagasdowa
password='@alexa001011#'
## Default options
protocol=dyndns2
server=members.dyndns.org
## Dynamic DNS hosts
ambagasdowa.dyndns.org

// For no-ip server
daemon=3600
mail-failure=ambagasdowa@gmail.com
syslog=yes
pid=/var/run/ddclient.pid
protocol=noip
login=ambagasdowa@gmail.com
password=alexa001011
use=web, web=ip1.dynupdate.no-ip.com/
server=dynupdate.no-ip.com/nic/update
ambagasdowa.sytes.net

#######################################################################
			## Debian Upgrade ##
 How to upgrade Debian Wheezy to Jessie safely
On this page

    Backup the configuration and data
    Check the apt sources.list file
    Update the packages for Wheezy
    Check the package state to ensure that no packages are on hold or in half installed state
    Update the sources.list for Jessie
    Jessie Upgrade in two steps
    Check the update
    Links

This tutorial explains the upgrade procedure to upgrade Debian from Wheezy to Jessie in a few simple steps.

Before you start with the upgrade, backup all critical data. Thats especially the config data in the /etc directory and the user data in /home. When you run a server then you should include the databases (MySQL, Postgresql, etc), web and email data in the backup as well. The backup should be stored on a external harddisk, on an external server e.g. by FTP or on a cloud drive. Here are a few examples on how to create backups with tar.
Backup the configuration and data

Configuration files in the /etc directory.

tar -pczf etc.tar.gz /etc

Backup of the /var/www website directory.

tar -pczf www.tar.gz /var/www

Backup of the /home directories.

tar -pczf home.tar.gz /home

For ISPConfig 3 users: Backup of your email server files.

tar -pczf vmail.tar.gz /var/vmail


// "add remote mariadb/mysql connection to sqldeveloper@oracle"
 ssh -L 3308:127.0.0.1:3306 systems@192.168.20.100

// teory

The tunneling command opens an SSH session with the [remote host] specified. The tunnel will work as long as that SSH session is active. If the session window
is consuming valuable workspace we recommend minimizing it as running tunnels in the background can lead to multiple tunnels and port conflicts.

// To create and test the SSH tunnel for a MySQL database

 1. In a terminal window on your local machine, type:

    ssh -L 3307:ec2-172-16-139-19.us-west-1.compute.amazonaws.com:3306 \
    deploy@ec2-174-129-17-196.compute-1.amazonaws.com

    where
    3307 is the local port,
    ec2-172-16-139-19.us-west-1.compute.amazonaws.com is the database host,
    3306 is the listening port,
    deploy is the database username, and
    ec2-174-129-17-196.compute-1.amazonaws.com is the remote host.

 2. Before connecting the external database tool such as MySQL Workbench or SQLyog, test the connection with a simple tool such as the database console,
    mysql.
    (Your tunnel needs to be running for this test.)

    Type

    mysql -udeploy -p -P 3307 -h 127.0.0.1

    where
    deploy is the database username on the remote host and
    3307 is the local port.

    You are prompted for your database password.

    Note: for the -h argument, it is necessary to use 127.0.0.1 instead of localhost.

// To create and test the SSH tunnel for a PostgreSQL database

 1. In a terminal window on your local machine, type

    ssh -L 5433:ec2-172-16-139-19.us-west-1.compute.amazonaws.com:5432 \
    deploy@ec2-174-129-17-196.compute-1.amazonaws.com

    where
    5433 is the local port,
    ec2-172-16-139-19.us-west-1.compute.amazonaws.com is the database host,
    5432 is the listening port,
    deploy is the database username, and
    ec2-174-129-17-196.compute-1.amazonaws.com is the remote host.

 2. Before connecting the external database tool such as pgAdmin, test the connection with a simple tool such as the database console psql.
    (Your tunnel needs to be running for this test.)

    Type

    psql -Udeploy -d myapp -p 5433 -h 127.0.0.1

    where
    deploy is the database username on the remote host and
    5433 is the local port.






// "¿Cómo crear un nuevo usuario in MySQL / MariaDB ?"

En la parte 1 del tutorial de MySQL, hicimos todas las ediciones como usuario root, con acceso completo a todas las bases de datos. Como sea, en este caso hay
mas restricciones que pueden ser requeridas, hay formas de crear usuarios con persmisos personalizados.

Vamos empezando por crear un usuario nuevo desde la consola de MySQL:

CREATE USER 'nombre_usuario'@'localhost' IDENTIFIED BY 'tu_contrasena';

Lamentablemente, a este punto el nuevo usuario no tiene permisos para hacer algo con las bases de datos. Por consecuencia si el usuario intenta identificarse
(con la contraseña establecida), no será capaz de acceder a la consola de MySQL.

Por ello, lo primero que debemos hacer es porporcionarle el acceso requerido al usuario con la información que requiere.

GRANT ALL PRIVILEGES ON * . * TO 'nombre_usuario'@'localhost';

Los asteriscos en este comando hacen referencia a la base de datos y la tabla (respectivamente) a la cual el nuevo usuario tendrá acceso; específicamente este
comando permite al usuario leer, editar, ejecutar y realizar todas las tareas en todas las bases de datos y tablas.

Una vez que has finalizado con los permisos que deseas configurar para tus nuevos usuarios, hay que asegurarse siempre de refrescar todos los privilegios.

FLUSH PRIVILEGES;

Tus cambios ahora surtirán efecto.


// other aproach
grant usage on portal_company.* to portal_company@localhost identified by '@portal_company#';
grant select, insert, update, delete, drop, alter, create temporary tables on portal_company.* to portal_company@localhost;
flush privileges;

grant usage on *.* to user@localhost identified by 'mypass';
grant all privileges on *.* to 'user'@'localhost'
flush privileges;


"mariadb/mysql user like root"
Creating a new user who can do anything (like root):

CREATE USER 'alexander'@'localhost';
GRANT ALL PRIVILEGES ON  *.* to 'alexander'@'localhost' WITH GRANT OPTION;


"Backup MySQL databases"

mysqldump --defaults-file=/etc/mysql/debian.cnf -cCeQ --hex-blob --quote-names --routines --events --triggers --all-databases -r all_databases.sql

Then move the tar.gz and .sql files for safe storage on a backup drive.

#Export:
mysqldump -u root -p --all-databases > alldb.sql
Look up the documentation for mysqldump. You may want to use some of the options mentioned in comments:

mysqldump -u root -p --opt --all-databases > alldb.sql
mysqldump -u root -p --all-databases --skip-lock-tables > alldb.sql

#Import:
mysql -u root -p < alldb.sql


One of the easiest ways I've found to export users is using Percona's tool pt-show-grants. The Percona tool kit is free, easy to install, and easy to use, with lots of documentation. It's an easy way to show all users, or specific users. It lists all of their grants and outputs in SQL format. I'll give an example of how I would show all grants for test_user:

shell> pt-show-grants --only test_user
Example output of that command:

GRANT USAGE ON *.* TO 'test_user'@'%' IDENTIFIED BY PASSWORD '*06406C868B12689643D7E55E8EB2FE82B4A6F5F4';
GRANT ALTER, INSERT, LOCK TABLES, SELECT, UPDATE ON `test`.* TO 'test_user'@'%';
I usually rederict the output into a file so I can edit what I need, or load it into mysql.

Alternatively, if you don't want to use the Percona tool and want to do a dump of all users, you could use mysqldump in this fashion:

shell> mysqldump mysql --tables user db > users.sql
Note: --flush-privileges won't work with this, as the entire db isn't being dumped. this means you need to run it manually.

shell> mysql -e "FLUSH PRIVILEGES"




Check the apt sources.list file

We will install all available Wheezy updates first before we upgrade to Jessie. On some systems the package source is defined as "stable" in the sources.list file instead of "wheezy" or "jessie". To avoid an accidential early upgrade to Jessie, please check the sources.list now and ensure that it contains "wheezy" and not "stable" as source:

Use a commandline editor like nano or vi to open /etc/apt/sources.list, the lines should be similar to the ones below:

nano /etc/apt/sources.list

deb http://ftp.de.debian.org/debian wheezy main contrib non-free
deb-src http://ftp.de.debian.org/debian wheezy main contrib non-free
deb http://ftp.de.debian.org/debian wheezy-updates main contrib non-free
deb http://ftp.de.debian.org/debian-security wheezy/updates main contrib non-free

Your country code is most likely not "de", you should use the code of your country here. Using your country code in the file will speed up the package download.


Update the packages for Wheezy

The following commands will install all pending Wheezy updates. This ensures that your system is in a good shape for the update to Jessie.

apt-get update

Ready for first upgrade:

apt-get upgrade

Follow this with:

apt-get dist-upgrade


Check the package state to ensure that no packages are on hold or in half installed state

This test is important, we will check the package state to ensure that no packages are on hold or in half installed state. Your system and apt database must be in a good shape before we proceed with the dist upgrade. If there are any broken or "on hold" packages, then fix these issues before the upgrade.

Check that no packages are on hold by querying the package database with the dpkg command:

dpkg --audit
dpkg --get-selections | grep hold

When both commands did not return any packages, then proceed with the upgrade.


Update the sources.list for Jessie

Edit the /etc/apt/sources.list file again:

nano /etc/apt/sources.list

and replace its content with the following lines:

deb http://ftp.de.debian.org/debian/ jessie main contrib non-free
deb-src http://ftp.de.debian.org/debian/ jessie main contrib non-free


deb http://httpredir.debian.org/debian jessie-updates main contrib non-free
deb-src http://httpredir.debian.org/debian jessie-updates main contrib non-free


deb http://security.debian.org/ jessie/updates main contrib non-free
deb-src http://security.debian.org/ jessie/updates main contrib non-free

Choose your nearest locale by replacing "de" with e.g. "us" or your country code. Then save the file and run the following command to update the sources database:

apt-get update


Jessie Upgrade in two steps

It is recommended to do the upgrade in two steps, first run "apt-get upgrade" to install the base packages and then run "apt-get dist-upgrade" to do the actual distribution upgrade,

Start with the update by running this command:

apt-get upgrade

Next we will do the distribution upgrade by running:

apt-get dist-upgrade

A reboot is required to finish the upgrade and load the new kernel:

reboot
Check the update

To check which Debian version is currently installed on the system, take a look at the file /etc/os-release.

cat /etc/os-release


###################################################################
"Check the speed beetwen 2 nic interfaces"
"speed check"

# Server side
$ nc -v -v -l -n -p 2222 >/dev/null
listening on [any] 2222 ...
connect to [192.168.1.1] from (UNKNOWN) [192.168.1.1] 37630
^C sent 0, rcvd -1586085888

# Client side
$ pv -t -r -a -b /dev/zero | nc -v -v -n 192.168.1.1 2222 >/dev/null
(UNKNOWN) [192.168.1.1] 2222 (?) open
too many output retries : Broken pipe
 sent -1586061312, rcvd 0
2.52GB 0:00:04 [ 571MB/s] [ 571MB/s]

####################################################################

example of rdesktop a client for RPD protocol
rdesktop -a 16 -x 1 -g 1200x900 -r sound:locale -u username 192.168.1.8 -k es

rdesktop -a 16 -x 1 -g 1440x720 -r sound:locale -r clipboard:CLIPBOARD -r printer:defaults -u Administrator 192.168.20.236 -k es

-a as -depth on vncserver -g as -geometry -u is user -x 1 as :1 and of course the server

-r for tunneling ex -r sound:locale -k es for spanish keyboard layout

using vncviewer
firts one lazzy example
vncserver -geometry 1200x900 -depth 32 :1

vncviewer //server:[xdisplay]

For windows
vncviewer -geometry 1024x780 -depth 16 -noshared -compresslevel 8 -quality 1 -encoding zlib -bgr233 -nocursorshape -x11cursor @IP
remember change xstartup on .vnc/ to your favorite desktop like startkde &

using sshfs of course

sshfs server mountpoint options
ex:
sshfs root@host /mnt -o umask=0

/** Connect to own server: */

 sudo sshfs ambagasdowa@baizabal.xyz:/var/www/baizabal.xyz/public_html /media/baizabal/ -o umask=0,allow_other,reconnect,auto_cache -o compression=no
/** Rdesktop Connect to mssql from work */

/**Mount with fstab**/
With systemd on-demand mounting is possible using /etc/fstab entries.

Example:

user@host:/remote/folder /mount/point  fuse.sshfs noauto,x-systemd.automount,_netdev,users,idmap=user,IdentityFile=/home/user/.ssh/id_rsa,allow_other,reconnect

The important mount options here are noauto,x-systemd.automount,_netdev.

  • noauto tells it not to mount at boot
  • x-systemd.automount does the on-demand magic
  • _netdev tells it that it is a network device, not a block device (without it "No such device" errors might happen)



rdesktop -a 16 -x 1 -g 1600x840 -r sound:locale -r clipboard:PRIMARYCLIPBOARD -r printer:defaults -u SistemasTBK -p S1st3m5s5 192.168.20.235 -k es -r disk:sharename

enjoy!!!
------playing with ssh
for forwading X

	ssh -X -C user@remote-host //-X for forwarding and -C for compression , then you can execute remote Xapps in your
					localhost  you must have X11Forwarding option to yes in sshd_config file
	ssh user@remote-host       //for execute remote Xapps in the remote host use command -display :0 & for kde-qt apps
				and --display :0 & for GTK-Gnome Xapss for example kdialog -msgtext "boo" -display :0 &

using 7za

"My example"

for compression with pass
7za a -pmy_pass -mhe=on -t7z -m0=lzma -mx=9 -mfb=64 -md=32m -ms=on archive.7z frm_dir

for extraction
7za x -pmy_pass -odir_to_output/ archive.7z

x=extract whit full path
a= add

7za a -t7z -m0=lzma -mx=9 -mfb=64 -md=32m -ms=on archive.7z dir1

Backup and limitations
       DO NOT USE the 7-zip format for backup purpose on Linux/Unix because :
        - 7-zip does not store the owner/group of the file.


       On Linux/Unix, in order to backup directories you must use tar :
        - to backup a directory  : tar cf - directory | 7za a -si directory.tar.7z
        - to restore your backup : 7za x -so directory.tar.7z | tar xf -

       If  you  want  to  send  files  and  directories  (not  the  owner  of  file)  to   others
       Unix/MacOS/Windows users, you can use the 7-zip format.

         example : 7za a directory.7z  directory


       Do not use "-r" because this flag does not do what you think.

       Do  not  use  directory/*  because  of  ".*" files (example : "directory/*" does not match
       "directory/.profile")   */
EXAMPLE 1
       7za a -t7z -m0=lzma -mx=9 -mfb=64 -md=32m -ms=on archive.7z dir1
              adds all files from directory "dir1" to archive archive.7z using "ultra settings"

       -t7z   7z archive

       -m0=lzma
              lzma method

       -mx=9  level of compression = 9 (Ultra)

       -mfb=64
              number of fast bytes for LZMA = 64

       -md=32m
              dictionary size = 32 megabytes

       -ms=on solid archive = on

EXAMPLE 2
       7za a -sfx archive.exe dir1
              add all files from directory "dir1"  to  SFX  archive  archive.exe  (Remark  :  SFX
              archive MUST end with ".exe")
EXAMPLE 3
       7za a -mhe=on -pmy_password archive.7z a_directory
              add  all  files from directory "a_directory" to the archive "archive.7z" (with data
              and header archive encryption on)

#####################################################################
fix the orange icon on hp downgrade

Djorge: Los controladores del lector de huellas digitales está en ftp://ftp.hp.com/pub/softpaq/sp39501-40000/sp39532.exe .
Con respecto al indicador de mute naranja, te paso la solución:
Primero nos aseguramos de que el proceso stacsv.exe se encuentre activo.
( CTRL+ALT+DEL , y de allí al administrador de procesos, allí verificamos eso )

Vamos a editar el registro

Inicio - Ejecutar - regedit

Presionamos F3 o CTRL+B y nos saldrá la búsqueda

Buscamos "AllowHpOnNonSupportingPin" sin las comillas

debe salir una RUTA de registro similar A ESTA

HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Contro l\Class\XXX\00yy\Settings]

(Las XXX) son un numero que varia según la instalación, así que esa parte no es exactamente igual a la mía.

Abrimos el notepad

Pegamos todo este texto: ( sin las ///////// )

//////////////////////////////////
Windows Registry Editor Version 5.00

[HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Contro l\Class\XXX\00yy\Settings]
"Installed"=hex:01
"CPL_SaveExtraPower"=hex:01
"CPL_SecondsAfter"=dword:00000010
"CPL_MaySwitchExtAmpPowerState"=hex:01
"AfgPowerStatePolicy"=hex:02
"CPL_ControlAfgPowerState"=hex:01
"CPL_SpdifTransmitterEnabledByUser"=hex:00
"CPL_FixedDigitalOutRate"=dword:00000000
"PmEnabledWidget"=hex:0a,0b,0c,0d,0e,0f
"EnablePcBeepAtCd"=hex:01
"PlaybackCaptureAssociations"=hex:51
"KeepAnalogLoopbackAlwaysOn"=hex:01
"DelayInMsAfterPortDisabling"=dword:00000040

[HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Contro l\Class\XXX\00yy\Settings\GPIO]
"03"=hex:c2

[HKEY_LOCAL_MACHINE\SOFTWARE\IDT\Config\StacSV]
"MuteGPIOIndicator"=dword:00000103
//////////////////////////////////

Cambiamos las XXX\00yy\ de ese texto, por las que correspondan a la version de ustedes.

Le damos a "Guardar como" , elegimos tipo de archivo - todos los archivos y le ponemos el nombre de archivo FIX.reg, y Guardamos.

Cerramos el editor de Registro.

Hacemos doble clic en el archivo, y añadimos la información al registro.

Reiniciamos la maquina.
/////////////////////////////////////////////////////////////////////////////////////////
winetricks

Primero, Abre tu terminal (Aplicaciones/accesorios/terminal)
Segundo, Escribe: sudo apt-get install cabextract (Se instala cabextract) No hacer nada hasta que termine
Tercera, Escriba lo siguiente: wget http://kegel.com/wine/winetricks (Se descarga un archivo llamado winetricks, no la elimine)
Cuarta, Escriba: sh winetricks corefonts dotnet20 (Se ejecutará winetricks, y luego te descargará e instalará .NET Framework 2.0). Esto es importante, no hacer nada hasta que termine. Cuando vea el asistente de instalación del .NET Framework, haga clic en Siguiente hasta terminar. Después de eso, la terminal   descargará las fuentes. Mantener la terminal abierta hasta que todo está hecho.

Quinta: para instalar el juego desde el archivo .msi escribir en la consola:

msiexec /i osu!install.msi

Ahora, intente ejecutar Osu.

Usted debe ser capaz de ejecutar Osu! ahora.

****************				*************								*******
installing directx

Instalar Directx 9:

Para empezar, van a tener que borrar la carpeta .wine o renombrarla en caso de no quere borrarla, que esta (oculta) en su carpeta de usuario (si tienen aplicaciones importantes o cosas así importenlas o guarden los instaladores).

Después vamos al configurador de wine, si no tienen el acceso en su panel de aplicaciones, escriban en la terminal:

winecfg
Pongan como ahi dice:d3d9
Version a imitar: Windows 2000

Ahora vamos a la pestaña ‘Librerías’:
Seleccionamos la librería que dice la imagen y le damos a ‘añadir‘, después la editamos para que diga ‘interna‘.

Una vez configurado todo a gusto, hacemos click en ceptar y se va a crear el directorio .wine otra vez.

Vallamos al directorio /home/usuario/.wine (en donde usuario es tu nombre de usuario) y editemos con algun editor de textos el archivo ‘user.reg‘

Busquemos la linea que diga:
/** NOTE links */
# "http://hyderomastgroningem.blogspot.mx/ //music ost"
